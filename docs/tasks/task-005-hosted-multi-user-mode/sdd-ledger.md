# SDD ledger — plan: docs/tasks/task-005-hosted-multi-user-mode/plan.md

Spec: docs/tasks/task-005-hosted-multi-user-mode/design.md (requirements prd.md, wire formats api-contracts.md)
Worktree: .worktrees/task-005-hosted-multi-user-mode (branch task-005-hosted-multi-user-mode)
Tasks: 29 (Task 29 is "code review before the PR" — handled by the final review step)

## Pre-flight scan

Full table: `.superpowers/sdd/plan/preflight-scan.md` (Table A 39 interface pairs, Table B 29 per-task rows).
No task violates the plan's Global Constraints (dependency direction, standalone equivalence, parameterised SQL, CGO_ENABLED=0, linters, error codes/statuses, env vars).

Rulings on the nine non-consistent rows:

1. Ruling: Task 18 `PATCH /api/settings/providers/{id}` with a `slug` key answers **400/INVALID_REQUEST** (the plan's reading), not api-contracts.md:308's 422/VALIDATION_ERROR — immutability is enforced by the patch attribute struct having no `Slug` field, so the existing `jsonapi.Decode` unknown-attribute path produces the status, consistent with every other endpoint; forcing 422 would require re-adding a `Slug` field plus a hand-written check. Task 28 must amend api-contracts.md to say so. Cost if wrong: clients expecting 422 for that one case see 400; reversible in one handler + doc edit.
2. Ruling: `auth.NewID` stays 16 hex chars (deviation #2). A user id becomes a directory name under REPOSITORY_CACHE_ROOT; a collision merges two users' mirrors. PRD §6.2's "same generator" is satisfied in scheme, not width. Cost if wrong: none observable.
3. Ruling: Task 19's `deleteReview` and `sessionFor` mode branches stand (deviations #3/#4). Both exist for non-disclosure, and standalone equivalence is requirement #1. Reviewers may flag them as design §7 violations; they are pre-ruled. Cost if wrong: two localized branches to refactor.
4. Ruling: api-contracts.md's missing INVALID_REQUEST/400 rows on the register and provider-settings error tables are a documentation gap, not a behaviour change. Fold into Task 28. Cost if wrong: docs lag one commit.
5. Ruling: Task 17/18 ship `TestAuthRoutesAre404InStandalone` / `TestSettingsRoutesAre404InStandalone` with `t.Skip`, un-skipped in Task 19 (Notes for the executor). Tasks 17/18 reviewers will correctly flag a skipped test as plan-mandated; pre-ruled — do not delete them, and Task 19 MUST un-skip both. Cost if wrong: two tests stay skipped — Task 19's review catches it.
6. Ruling: Task 5's `TestVerifyReadsParamsFromTheString` calls `argon2.IDKey` directly on purpose; `HashPassword` always uses the package constants so a round-trip could never prove parameters are read back. Do not "simplify" it. Cost if wrong: none.
7. Ruling: Task 14's timing-equalisation test must NOT ship as a 3-sample wall-clock assertion — that is a flaky CI test. The implementer takes the smallest robust formulation: assert the structural property (an unknown user still runs a dummy Argon2id verification) and, if a timing assertion is kept, use a median over >=15 samples with a wide tolerance. Cost if wrong: a deviation from the plan's test text, in the safe direction.
8. Ruling: `ProviderPatch.Validate` defaults to `false` and `ProviderInput.Validate` to `true` (deviation #5, hence `*bool` on create). Cost if wrong: a cosmetic PATCH skips a provider-API round trip.
9. Resolved, not a finding: Task 26's `secrets_test.go` assumes `app.NewLogger(w io.Writer, cfg config.Config) *slog.Logger` — verified present at `apps/backend/internal/app/app.go:173`. No action.

## Progress

Ruling: dispatched Task 1 concurrently with the pre-flight scan — Task 1 creates a stdlib-only leaf package that nothing else exists to conflict with, and the scan's findings cannot change it. Cost if wrong: one discarded leaf-package commit.
Task 1: dispatched (base cb6f785, model sonnet, brief .superpowers/sdd/plan/task-1-brief.md)
Task 1: complete (commits cb6f785..8d1cfb0, review clean)
Ruling: dispatched Task 2 before the pre-flight scan returned — Task 2 (config) touches only internal/config, its values are pinned verbatim in the plan's Global Constraints, and any scan finding against it surfaces in its own review. Cost if wrong: one fix round on config.
Task 2: dispatched (base 8d1cfb0, model sonnet, brief .superpowers/sdd/plan/task-2-brief.md)
Task 2: minor (deferred): doc comment for loadProviders sits above modeVar instead (apps/backend/internal/config/config.go:218-225 vs :254); loadProviders has no doc comment.
Task 2: complete (commits 8d1cfb0..7868fe6, review clean — approved, minors only)
Task 3: dispatched (base 7868fe6, model sonnet, brief .superpowers/sdd/plan/task-3-brief.md)
Task 3: docker-build verified early (exit 0, image 69.6MB / 19.2MB compressed, cmd/converge binary 11.6MiB, CGO_ENABLED=0) — the design's named modernc.org/sqlite size risk is cleared.
Task 3: reviewer ⚠️ on commit trailers resolved by controller — `git log -3 --format=%(trailers)` shows the required Co-Authored-By trailer on all three commits.
Task 3: minor (deferred): db.Open builds the DSN by concatenating CONVERGE_DATABASE_PATH into a `file:` URI unescaped (apps/backend/internal/db/db.go, Open) — operator-supplied, not client input, but a path containing ?/#/space would break DSN parsing.
Task 3: complete (commits 7868fe6..e7d1494, review clean — approved, minors only)
Task 4: dispatched (base e7d1494, model sonnet, brief .superpowers/sdd/plan/task-4-brief.md)
Task 4: reviewer ⚠️ resolved by controller — auth.NormalizeBaseURL matches config.buildProvider (apps/backend/internal/config/config.go:354-365): same gitlab-BASE_URL-required rule, same http(s)+host check, same TrimRight("/"); auth additionally TrimSpaces the input, a strict superset. No divergence.
Task 4: minor (deferred): User.UsernameFold() and NewLoginSession are ahead of the brief's Interfaces list (unused surface); errors.go package doc states the eventual import boundary as current fact when only config is imported so far.
Task 4: complete (commits e7d1494..23fb4bc, review clean — approved, minors only)
Task 5: dispatched (base 23fb4bc, model sonnet, brief .superpowers/sdd/plan/task-5-brief.md), commit 4b1b544
Task 5: review — 1 Important (decodePHC has no upper bound on m/t/p read from the stored hash; a corrupt row drives a fatal OOM or an unbounded hang inside argon2.IDKey), 3 Minor.
Ruling: fix the Important with explicit ceilings in decodePHC — memory <= 1<<20 KiB, timeCost <= 16, threads <= 16 (16x/16x/4x the FR-2.3 production values), rejected as a malformed encoding. Cost if wrong: a legitimate future parameter bump above those ceilings needs the constants raised in the same commit.
Task 5: fix round 1/5 dispatched (fresh implementer — this harness cannot resume a finished subagent; carries brief + report + all 4 findings)
Task 5: fix round 1/5 (4 addressed, 0 open; commits 4b1b544..01cf6e2, re-review clean)
Task 5: minor (deferred): TestVerifyRejectsOversizedCostParameters uses a fixed 200ms wall-clock timeout to prove no hang — 4x margin over a ~50ms hash, but latent flakiness on a loaded CI runner.
Task 5: complete (commits 23fb4bc..01cf6e2, review clean after 1 fix round)
Task 6: dispatched (base 01cf6e2, model sonnet, brief .superpowers/sdd/plan/task-6-brief.md), commit 6dd7762
Task 6: reviewer ⚠️ resolved by controller — the NUL-separated AAD is collision-free because both ids come from auth.NewID, which is `hex.EncodeToString` of 8 crypto/rand bytes (apps/backend/internal/auth/model.go): 16 chars from [0-9a-f], so neither operand can contain 0x00.
Task 6: minor (deferred): aad()'s doc comment states the NUL separator's unambiguity unconditionally rather than noting it rests on NUL-free ids; the two tamper tests assert only err != nil without also checking the returned secret is zero; Seal validates empty user/provider ids but Open does not.
Task 6: carry-forward for Task 15 — reviewer could not verify from this diff that Seal/Open are actually called with user_id + the provider ROW id (not some other identifier pair). Task 15's review must confirm the call sites.
Task 6: complete (commits 01cf6e2..6dd7762, review clean — approved, minors only)
Task 7: dispatched (base 6dd7762, model sonnet, brief .superpowers/sdd/plan/task-7-brief.md), commit 0df1f52
Task 7: reviewer ⚠️ (schema FK cascade / UNIQUE constraints not visible in this diff) resolved by controller — Task 3's review already verified 0001_init.sql column-by-column against PRD §6.2, including login_sessions/user_providers FK ON DELETE CASCADE and login_attempts' composite PK.
Task 7: NOTE for whoever changes the DB pool — uniqueness is enforced by a check-then-insert inside a transaction, which is race-free ONLY because internal/db sets MaxOpenConns(1). Raising the connection limit breaks USERNAME_TAKEN/PROVIDER_SLUG_TAKEN correctness; the raw constraint violation surfaces unmapped.
Task 7: minor (deferred): SetPasswordHash / TouchLoginSession / DeleteLoginSession / DeleteOtherLoginSessions ignore RowsAffected and silently no-op on a missing row; no test covers DeleteUser or UpdateUserProvider returning ErrNotFound (code paths are correct by inspection); store.go is 407 lines covering four tables and will grow.
Task 7: carry-forward for Task 15 — confirm Task 15's provider CRUD can build a UserProvider through the NewUserProvider signature added here (apps/backend/internal/auth/model.go:184-211) without needing a second constructor.
Task 7: complete (commits 6dd7762..0df1f52, review clean — approved, minors only)
Task 8: dispatched (base 0df1f52, model sonnet, brief .superpowers/sdd/plan/task-8-brief.md), commit f8dc70b
Task 8: review — 1 Important, labeled plan-mandated: Throttle.Fail does a read-modify-write across two store calls (Attempt then SaveAttempt), so with MaxOpenConns(1) releasing the connection between them, two concurrent Fail calls on one key lose an increment. The brief's own reference code has this shape.
Ruling: fix it despite being plan-mandated. A lost-update race in a brute-force control defeats the control exactly when it matters, and the fix is cheap: a new Store.UpdateAttempt(ctx, scope, key, fn) doing SELECT + policy callback + upsert inside ONE transaction, with the doubling policy staying in throttle.go as a pure-arithmetic callback. Plus a 20-goroutine concurrent-Fail test asserting an exact count. Cost if wrong: one extra store method and a slightly wider transaction holding the sole connection for pure arithmetic.
Task 8: minor (deferred): Sweep's "just expired" boundary is untested (only a 1-hour-stale lockout); a backwards-moving clock is untested.
Task 8: fix round 1/5 (3 addressed, 0 open; commits f8dc70b..32f668e, re-review clean — Store.UpdateAttempt is now one BeginTx→SELECT→callback→upsert→Commit, callback is pure arithmetic, no nested pool call, schedule constants byte-identical)
Task 8: complete (commits 0df1f52..32f668e, review clean after 1 fix round)

## HANDOFF at Task 8 (2026-09-10)

Controller hit the ~150k context-handoff threshold at 61 tool calls, which coincides with the plan's own first natural boundary ("after Task 8 — the auth primitives"). Tasks 1-8 are complete, reviewed, and committed. Next session resumes at **Task 9** (`provider.Resolver` and the mechanical rewire).
Resume by: `/clear`, then `/execute-task task-005` from the worktree. This ledger is the recovery map; the commits it names exist in git.
Base for Task 9 is `32f668e`. Tasks 9-12 are the signature churn the design sequences early — do not reorder them.
Task 5: carry-forward for Task 14 — reviewer could not verify that callers hash OUTSIDE a transaction and under the four-way semaphore (the contract in HashPassword's doc comment). Task 14's dispatch and review must check it.

## Session 2 (2026-09-11) — resumed at Task 9

Task 9: dispatched (base 9a7b07b, model sonnet, brief .superpowers/sdd/plan/task-9-brief.md)
Task 9: implementer DONE, commit 461ea8b. Brief conflict resolved by implementer per the brief's own "check the real signature" instruction: fake.New takes (id string, kind provider.Kind), not one arg.
Task 9: task reviewer dispatched (model sonnet, range 9a7b07b..461ea8b)
Task 9: reviewer ⚠️ was only "did not re-run the suite" (my own instruction — the implementer's report carries the gate evidence), not a gap.
Task 9: minor (deferred): service.go:544 and service.go:571 duplicate the literal "review: resolve providers: %w" in two near-identical resolve-check-wrap blocks; Task 12 touches both sites again and could fold in a helper.
Task 9: complete (commits 9a7b07b..461ea8b, review clean — approved, minors only)
Task 10: dispatched (base 461ea8b, model sonnet, brief .superpowers/sdd/plan/task-10-brief.md)
Task 10: implementer DONE, commit efc8e3d. Brief conflicts reported: (1) brief's sample used a non-existent buildSession(t) fixture — real helper is newSession(t); (2) Purge drops the RemoveDir call, per the brief's own implementer note, because review.Cleaner.Cleanup / workspace.Manager.Cleanup already removes the session directory unconditionally.
Task 10: review — spec OK, quality approved, 1 Important (labeled plan-mandated), 1 Minor. Important: Store.Purge deletes index[id] AND pendingCleanup[id] up front (store.go:293-297) and on a Cleaner.Cleanup failure only appends to errs — it never re-registers the id. Sweep's retry loop needs the id in BOTH pendingCleanup and index (sweep.go:22-26), so a transient cleanup failure during account deletion strands the deleted user's session.json (owner field + review metadata) on disk with no in-process retry until a restart lets LoadAll rediscover it. Bears on FR-2.7. Finish/expire handle the same failure correctly via persistCleanupFailure (store.go:338-347).
Ruling: fix it now in Task 10 rather than defer to Task 14. It is real and it is load-bearing for FR-2.7, the fix lives entirely in internal/session/store.go (the file this task owns), and it reuses the persistCleanupFailure + Sweep retry path already proven for Finish — no new machinery. Deferring would mean re-opening the session package that Tasks 11-13 build on top of. A purged session whose cleanup fails stays indexed under a DELETED user's id, which no live scope can match, so retaining the index entry discloses nothing. Cost if wrong: one extra branch in Purge plus one test; a failed purge lingers in the in-memory index (invisible to every live scope) until Sweep succeeds, instead of disappearing from the index immediately while its files remain on disk.
Task 10: minor (deferred): TestPurgeRemovesEveryOwnedSession covers only the success path — no test exercises a Cleanup failure during Purge.
Task 10: fix round 1/5 dispatched (resumed the original implementer)
Task 10: NOTE — this harness has SendMessage disabled, so fix rounds cannot resume a live implementer. Every fix round dispatches a FRESH task-implementer carrying the brief path, the report path, and the findings; the report file is the persistent memory.
Task 10: fix round 1/5 — implementer DONE, commit 799f3a3 (Purge now routes a failed Cleaner.Cleanup through persistCleanupFailure so Sweep retries; new TestPurgeCleanupFailureIsRetriedBySweep, mutation ratio 1/1; sweep.go untouched).
Ruling correction (mechanism, not decision): my ruling's rationale said a retained purge entry is invisible because "no live identity.Scope can match a deleted user id". The implementer found that is false at the store layer — identity.Scope.Matches is a pure string compare, so Get(id, identity.ForUser(deletedID)) WOULD return it. The decision stands, but the actual guarantee is one layer up: the auth layer never reissues a deleted user's scope because the user row and its login sessions are gone. Recorded so Task 12/14 reviewers do not rely on a store-level guarantee that does not exist.
Task 10: re-review dispatched (scoped to efc8e3d..799f3a3)
Task 10: fix round 1/5 (2 addressed, 0 open; commits efc8e3d..799f3a3, re-review clean — persistCleanupFailure reused, sweep.go untouched, success path unchanged, joined error still returned, new test asserts retry AND recovery, and the replacement comment describes the real auth-layer guarantee rather than a store-level one)
Task 10: complete (commits 461ea8b..799f3a3, review clean after 1 fix round)
Task 11: dispatched (base 799f3a3, model sonnet, brief .superpowers/sdd/plan/task-11-brief.md)
Task 11: implementer DONE, commit 0494dc6. Brief self-conflict reported: the rejection table lists "" as a case that must return an error, but the brief's own NamespaceFor reference code treats an empty user id as the standalone scope (no error).
Ruling: the reference code is right and the table row is the plan defect — uphold the implementer. Verified in apps/backend/internal/identity/scope.go: Scope holds one unexported userID, Standalone() returns the zero value, and ForUser("") therefore returns a Scope bit-identical to Standalone() with IsScoped() == false. Making NamespaceFor error on an empty id would make NamespaceFor(identity.Standalone()) error, breaking standalone equivalence (requirement #1, FR-6.5). The six non-empty bad ids still reject, and "" is asserted to yield the root namespace in its own subtest. Cost if wrong: nothing security-relevant — an empty id cannot arrive from a verified login session, since ForUser is only called by the auth middleware with a session's user id; if a caller ever needs "empty id is a bug, not standalone", that belongs in identity.Scope, not in mirror.
Task 11: review — spec OK, quality approved, 0 Critical/0 Important. Security boundary verified: userIDRe = ^[0-9a-f]{16}$ is positive exact-length validation (cache.go:95), the containment test resolves a real path from the returned Namespace and proves via filepath.Rel it cannot escape the cache root (cache_test.go:505-516), PurgeNamespace refuses ns.IsRoot() before any os.RemoveAll (cache.go:170-172), no //nolint anywhere, AuthorizeGit/refspec path untouched.
Task 11: reviewer ⚠️ ("could another production call site outside the three named ones still use the old signature?") resolved by controller — a signature change cannot compile against an unconverted call site, and CGO_ENABLED=0 go build ./... plus the full race suite are green in the report. Not a gap.
Task 11: minor (deferred): NamespaceRoot is exported but absent from the brief's Produces list (it IS in the brief's Step 3 reference code, so not undocumented surface — just newly public API); NamespaceFor's "invalid user id" error omits the rejected id unlike Path's %q sibling, a defensible anti-log-injection choice that deserves a one-line comment so nobody "fixes" the asymmetry.
Task 11: complete (commits 799f3a3..0494dc6, review clean — approved, minors only)
Task 12: dispatched (base 0494dc6, model sonnet, brief .superpowers/sdd/plan/task-12-brief.md)
Task 12: implementer DONE_WITH_CONCERNS, commits e145057 + 89a50a7 (the second is a self-review fix: TestCreateResolvesTheCallersRegistry did not actually discriminate). Concerns are observations, not correctness gaps: (1) the mutation sweep was run fully only for Test 3 (1/1, verified in a scratch copy that reverting Create's scope to identity.Standalone() causes a genuine assertion failure, not just a build error); tests 1,2,4-7 rest on passing plus code inspection. (2) internal/api/*.go and cmd/converge-cli/main.go now pass identity.Standalone() explicitly with comments marking the placeholder. No brief-vs-reality mismatches found.
Task 12: task reviewer dispatched (model sonnet, range 0494dc6..89a50a7)
Task 12: review — spec OK, quality approved, 0 Critical/0 Important, 2 Minor. Reviewer independently confirmed the isolation seam: scopedRegistryResolver ERRORS (rather than returning an empty registry) for an unrecognised key, so reverting Create's scope to identity.Standalone() yields a bare wrapped error and the test's errors.As(*InputError) check fails — 89a50a7 is a real fix. PurgeUser joins and propagates both the store-purge and namespace-purge errors; mirror.NamespaceFor's error is propagated with no root fallback; no auth import in review or cmd/converge-cli (grep-verified); all pre-existing test hunks are mechanical argument insertions only.
Task 12: PLAN TEXT ERROR (also repeated in my own Task 12 dispatch, and inherited from task-12-brief.md Step 4): the brief says "Task 14 replaces the API's placeholder with scopeFrom(r)". It is TASK 16 ("api — scope context, cookies, and the two middlewares", plan.md:4553) that owns scopeFrom, not Task 14 (auth/service.go, the account lifecycle). Harmless in shipped code — the committed comments in reviews.go/review_files.go/main.go cite no task number, only "a later task derives the caller's scope". TASK 16's dispatch must be told it owns the identity.Standalone() placeholders in internal/api/*.go. cmd/converge-cli's Standalone() call is PERMANENT (the CLI is standalone by nature), not a placeholder.
Task 12: minor (deferred): mutation coverage for new tests 1,2,4-7 rests on code inspection rather than an executed scratch mutation run (the reviewer independently traced all six through Scope.Matches / NamespaceFor / PurgeNamespace / store.List's IsActive filter and judged each would genuinely fail under the named regression); a later reviewer with budget should spot-check PurgeUser and the build/scopeOf path.
Task 12: reviewer ⚠️ (auth.Purger / auth.ProviderUsage do not exist yet, so structural satisfaction is unconfirmable) resolved by controller — correct and expected: those interfaces are declared in Task 14. TASK 14's review must confirm review.Service actually satisfies them, since review cannot import auth to assert it.
Task 12: complete (commits 0494dc6..89a50a7, review clean — approved, minors only)

### Boundary: Tasks 9-12 (the signature churn) are done. review's signatures are now stable.
Task 13: dispatched (base 89a50a7, model sonnet, brief .superpowers/sdd/plan/task-13-brief.md)
Task 13: implementer DONE, commit 7339b2c. Mutation ratios 1/1 on cache-key isolation (TestResolveIsolatesUsers) and 1/1 on the verify auth-mapping (TestHTTPVerifierMapsAuthFailure). One declared deviation: TestResolveSurfacesADecryptFailure corrupts the ciphertext in memory and writes it back through Store.UpdateUserProvider rather than a literal SQL UPDATE, because only store.go may contain SQL. No brief-vs-reality mismatches.
Task 13: task reviewer dispatched (model sonnet, range 89a50a7..7339b2c) — also carries the Task 6 and Task 7 carry-forwards, since this is the first real Sealer.Open call site.
Task 13: review — spec OK, quality approved, 0 Critical/0 Important. Cache isolation verified (resolver.go:66,73,83 keyed by scope.UserID() under RWMutex; TestResolveIsolatesUsers checks the other user's slug is ABSENT from each registry). Verify mapping verified: errors.Is(err, provider.ErrAuth) -> CodeProviderUnauthorized -> 422 (errors.go:64); every other error wrapped and returned as itself, so an outage cannot be reported as a bad credential. No log/slog/Printf in either new file; config.Secret passes unopened from Sealer.Open into github.New/gitlab.New. No SQL outside store.go. ListUserProviders is a plain QueryContext with no BeginTx, so no HTTP happens inside a transaction. Imports are stdlib + config/identity/provider/provider-github/provider-gitlab only.
Task 13: CARRY-FORWARD FROM TASKS 6 AND 7 DISCHARGED — resolver.go:104 calls r.sealer.Open(row.TokenCiphertext(), row.TokenNonce(), row.UserID(), row.ID()), matching Sealer.Open(ciphertext, nonce, userID, providerID) at crypt.go:78. row.ID() is the row's own auth.NewID(), not the slug. Provider rows round-trip through the existing auth.NewUserProvider with no second constructor.
Task 13: deviation upheld — the reviewer read Store.UpdateUserProvider (store.go:299-315) and confirmed it writes tokenCiphertext/tokenNonce byte-for-byte through a parameterised UPDATE with no re-sealing or normalisation, so TestResolveSurfacesADecryptFailure's in-memory corruption genuinely fails GCM authentication at Open. Not a no-op test.
Task 13: unscoped-scope decision recorded — Resolve returns a plain errors.New (not a Code-bearing *auth.Error) when scope.UserID() == "", which covers both identity.Standalone() and identity.ForUser(""). Code and doc comment agree ("failing loudly beats silently answering with nothing or with everything"); it never falls through to another user's cached registry. Pinned by TestResolveRejectsAnUnscopedScope. A plain error is right because the closed Code enum has no "caller misconfigured itself" value and this is a wiring bug unreachable from an authenticated request.
Task 13: reviewer ⚠️ x2 resolved by controller — (a) "standalone equivalence not verifiable from this diff": correct and expected, the diff adds three new unwired files under internal/auth; hosted wiring is Task 20, whose review must prove it. (b) "commit trailer not verified": verified by controller, `git log --format=%(trailers:key=Co-Authored-By,valueonly) 0494dc6..HEAD` shows the required trailer on all three of this session's commits (e145057, 89a50a7, 7339b2c).
Task 13: complete (commits 89a50a7..7339b2c, review clean — approved, no minors)

## HANDOFF at Task 13 (2026-09-11)

Session 2 ran Tasks 9-13. Controller stopping at the context-handoff threshold, on a clean boundary: the Tasks 9-12 signature-churn block is finished and review's signatures are stable, and Task 13 added the hosted resolver. Next task is **Task 14** (auth/service.go, the account lifecycle) — the largest brief so far (413 lines).
Resume by: `/clear`, then `/execute-task task-005` from the worktree. Base for Task 14 is `7339b2c`. Briefs for Tasks 10-15 are already extracted in this workspace.

## Session 3 (2026-09-11) — resumes at Task 14
Task 14: dispatched (base 705681f, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-14-brief.md)
Task 14: implementer DONE, commit f3c04f2. Purger/ProviderUsage confirmed byte-for-byte matching review.Service.PurgeUser/ProviderInUse — Task 12's carry-forward DISCHARGED, no Task 15/20 blocker. Mutation ratios 3/3 (ChangePassword session revocation, DeleteAccount purge-failure swallow, Authenticate idle-expiry disjunct). One declared deviation: the brief's test 15 prose names a Service.ListProviders that does not exist until Task 15; the implementer used Store.ListUserProviders directly in the concurrency test.
Ruling: the deviation stands — Store.ListUserProviders exercises the same single-connection read/write race the brief intends, and the brief's reference to Service.ListProviders is a forward reference to Task 15 (plan.md:4262, "provider configuration CRUD"), i.e. a plan-text defect, not a missing requirement. Cost if wrong: the concurrency test covers the store layer rather than the service wrapper, so a Task 15 service-level locking mistake would go uncaught here; Task 15's own review must cover ListProviders under concurrency. Fold the plan-text correction into Task 28 alongside the Task 12 one.
Task 14: task reviewer dispatched (model sonnet, range 705681f..f3c04f2)
Task 14: review — spec OK, quality approved, 0 Critical/0 Important, 3 Minor. Argon2id carry-forward from Task 5 DISCHARGED: hash (service.go:92-95) and verify (service.go:97-100) are the package's only HashPassword/VerifyPassword callers, both acquire s.hashSem and release via defer so the slot survives an early error return; all five call sites (Register:130, Login:182, ChangePassword:281,290, DeleteAccount:316) hash strictly before the following store write, and store.go's three BeginTx sites (42 CreateUser, 234 CreateUserProvider, 383 UpdateAttempt) neither hash nor sit between a hash and its store call — no transaction spans a hash. Purger/ProviderUsage independently re-verified against review/service.go:647 and :665, identical signatures, and review's import block carries no auth import. Login oracle closed two ways: dummy-hash substitution on unknown username plus a shared invalidCredentialsMessage (service.go:174-190), and Throttle.keys (throttle.go:135-144) keys by folded username regardless of existence with an identical lockedMessage for user- and IP-scope locks, so lockout cannot leak account existence.
Task 14: reviewer ⚠️ (MaxOpenConns(1) not verifiable from this diff) resolved by controller — verified directly: apps/backend/internal/db/db.go:71-72 calls SetMaxOpenConns(1) and SetMaxIdleConns(1). Established by Task 3, unchanged. Not a gap.
Task 14: test-15 substitution ruling upheld on mechanism — the reviewer confirmed TestConcurrentLoginAndProviderListDoNotDeadlock races Login (semaphore-gated verify + UserByFold/CreateLoginSession/throttle writes) against Store.ListUserProviders across 8 goroutines on the single connection, so it does exercise a concurrent read against the write path. Reviewer's qualifier recorded: because hashing sits outside any transaction, the test is a deadlock/liveness net rather than a tight proof that misordering fails loudly. Soft premise, not a false one.
Task 14: minor (deferred): s.hashSem <- struct{}{} (service.go:92-100) is an unconditional blocking send with no ctx.Done() select, so a canceled caller queued behind 4 hashes still waits for a slot; no test covers throttle lockout on a NONEXISTENT username specifically (oracle-freedom currently rests on code inspection of Throttle.keys rather than a regression test); Logout (service.go:238-244) does an extra Store.LoginSession read purely to recover user_id for its log line.
Task 14: complete (commits 705681f..f3c04f2, review clean — approved, minors only)
Task 15: dispatched (base f3c04f2, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-15-brief.md)
Task 15: implementer DONE, commit ce737be. Mutation ratios 5/5 (FR-5.5 token-keep bypass, FR-5.7 in-use bypass, FR-5.3 AAD wrong-id seal, sweeper lockout-skip, missing resolver invalidation). No brief conflicts. Added TestConcurrentListProvidersAndCreateProviderDoNotDeadlock beyond the brief's 15 tests, per controller carry-forward #1 (the obligation created by Task 14's test-15 ruling), plus two additive test-only fixture helpers (newServiceWithVerifier, newServiceWithUsage).
Task 15: task reviewer dispatched (model sonnet, range f3c04f2..ce737be) — carries the ListProviders-under-concurrency obligation from the Task 14 ruling.
Task 15: review — spec OK, quality approved, 0 Critical, 1 Important (about a ruling's wording, not the code), 2 Minor. Verified: AAD binds to the row's own id on create (providers.go:84) and re-seal on update (providers.go:163), proven by TestUpdateProviderReencryptsUnderTheSameRowAAD (providers_test.go:595-624, Open fails under a different user id post-update). Verification never spans a transaction — Verifier.Verify at providers.go:74,147,157 all precede the store write; CreateUserProvider's BeginTx (store.go:234) opens after verification returns and UpdateUserProvider (store.go:299) is a bare ExecContext. Cross-user isolation is in the SQL WHERE clause on all four paths (store.go:279,293,303,322), cross-user access surfaces ErrNotFound per FR-4.2 (errors.go:35-37). Cache invalidation present on all three mutation success paths (providers.go:100,174,197) and correctly absent on error returns. No SQL and no token/ciphertext/nonce in any log line (providers.go:101-102,175-176,198-199 log only user id, slug, kind, and a bool).
Task 15: Ruling on the Important finding — the Task 14 ListProviders-under-concurrency obligation is satisfied, and I am closing it rather than opening a fix round. The reviewer is right that my condition was worded for a risk that does not exist here: Service has no mutex of its own (service.go:77-79 holds only hashSem, which CreateProvider/ListProviders never touch), and ProviderResolver.mu is taken only around a map delete in Invalidate (resolver.go:92-95), never across a DB call. So "a lock held across a DB call" is structurally absent from the CRUD path, not merely unverified. What TestConcurrentListProvidersAndCreateProviderDoNotDeadlock (providers_test.go:823-860) does cover is the real risk on this pool: CreateUserProvider's BeginTx (store.go:234) racing ListUserProviders' bare QueryContext (store.go:267) under MaxOpenConns(1). That is the service method against a write on the single connection, which is what the obligation was for. Accepting as sufficient. Cost if wrong: if Tasks 16-20 later introduce a mutex into the provider CRUD path, no test catches a lock held across a DB call — so any task that adds a lock there must add its own deadlock test.
Task 15: CARRY-FORWARD FOR TASK 18 (binding, from reviewer Minor 2 + controller verification) — providers.go:70-72 returns a bare errors.New("auth: token is required") rather than a Code-bearing *auth.Error, and api.classify's fallback (errors.go:65) maps an untyped error to 500 GIT_FAILURE. api-contracts.md:268 says "token is required and non-empty" and :275 lists VALIDATION_ERROR (422) for it. So an empty token today would answer 500 instead of the contracted 422. The contract puts this in request-body validation, so TASK 18's handler must reject an empty token with VALIDATION_ERROR 422 before calling the service, and Task 18's review must verify it with a test. Not a Task 15 fix round: the service-layer guard is defence in depth behind a handler that the contract says owns the check. Cost if wrong: if Task 18 skips it, an empty token returns 500 and violates the documented contract.
Task 15: minor (deferred): sweep.go:12-15 returns early if DeleteExpiredLoginSessions errors, so a transient session-store error also delays that cycle's lockout cleanup (next cycle retries both; consider errors.Join to make the two housekeeping jobs failure-independent).
Task 15: reviewer ⚠️ (Throttle.Sweep / DeleteElapsedAttempts / DeleteExpiredLoginSessions correctness) resolved by controller — all three are pre-existing from Tasks 7 and 8 and untouched here; sweep.go only wires them together. Their own tests landed with those tasks. Not a gap in this diff.
Task 15: complete (commits f3c04f2..ce737be, review clean — approved, minors only)
Task 16: dispatched (base ce737be, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-16-brief.md)
Task 16: implementer DONE_WITH_CONCERNS, commit beb27ff. Mutation ratios: originGuard host-check flip caught by 2/5 originGuard tests, clientIP trusted-proxy bypass caught by 1/1. Three concerns: (1) added an *auth.Error arm to api/errors.go classify, a file outside the brief's stated scope, because the brief's own tests need the right code not just a non-2xx; (2) two //nolint:gosec with recorded reasons on the dynamic cookie Secure field; (3) TestAuthenticateRejectsAnUnknownToken asserts Max-Age=0 against the raw Set-Cookie text because Go's parser maps wire Max-Age=0 back to MaxAge=-1.
Task 16: CARRY-FORWARD #1 FROM TASKS 9-12 DISCHARGED — controller verified every production identity.Standalone() placeholder in internal/api is gone, replaced by scopeFrom(r): providers.go:23, repositories.go:42, reviews.go:91,105,125,152, review_files.go:40,67,86. The only remaining Standalone() in internal/api is authctx.go:34, which IS the standalone fallback itself (correct, and pinned by authmw_test.go:91-92). cmd/converge-cli/main.go:82,114,133 untouched as intended — permanent, the CLI is standalone by nature.
Ruling: concern (1) is accepted, not a scope violation to unwind. Plan Task 17 Step 1 ("Extend classify", plan.md:4846) owns the classify change, but Task 16's tests cannot assert the contracted 401/403 codes without it, and a test that accepts "any non-2xx" would be the weaker artifact. Cost if wrong: nothing in shipped code — the arm is needed identically by both tasks. TASK 17's dispatch must be told its Step 1 may already be satisfied, and to VERIFY the existing arm against the contract rather than add a duplicate one.
Task 16: task reviewer dispatched (model sonnet, range ce737be..beb27ff)
Task 16: review — spec OK, quality approved, 0 Critical/0 Important, 2 Minor. Security boundary verified: authenticate (authmw.go:202-220) is fail-closed on every path — the reviewer traced auth.Service.Authenticate (service.go:244-271) and confirmed missing/malformed/unknown/expired/idle-expired cookie and DB error ALL return non-nil, and the err != nil branch clears the cookie, writes the error, and returns without calling next.ServeHTTP. No fallthrough, no scope attached on failure. Cookie set/clear share identical Path/HttpOnly/SameSite/Secure derivation (authctx.go:93-117) so a browser honours the delete. clientIP defaults deny (authctx.go:122-136): with TrustedProxy=false the header is never read; with it true the LAST hop is taken, matching FR-7.3's single-proxy model. No SQL, no cookie/token in any log line.
Task 16: classify arm independently verified against auth/errors.go:55-70 — UNAUTHENTICATED/INVALID_CREDENTIALS 401, FORBIDDEN 403, USERNAME_TAKEN/PROVIDER_SLUG_TAKEN/PROVIDER_IN_USE 409, INVALID_USERNAME/WEAK_PASSWORD/PROVIDER_UNAUTHORIZED 422, ACCOUNT_LOCKED 429. Matches the Global Constraints table verbatim; added ahead of the pre-existing ie/de/re arms without altering them.
Task 16: the two //nolint:gosec suppressions upheld — the reviewer REPRODUCED the lint independently in a scratch copy with the comments stripped and confirmed G124 fires on both cookie literals solely because Secure is a dynamic expression; HttpOnly and SameSite are literal and correct. Genuine false positive, not masking a gap. The Max-Age=0 raw-header assertion also upheld: Go's parser maps wire Max-Age=0 to the MaxAge=-1 sentinel, so the raw Set-Cookie text is the only way to assert the real wire output.
Task 16: originGuard mutation "misses" explained and accepted — of 5 tests, AllowsSafeMethods and IgnoresNonAPIPaths return before the host-compare line executes, and RejectsAMissingOriginAndSecFetchSite never enters the origin != "" branch. Genuine short-circuits, not weak tests.
Task 16: BINDING CARRY-FORWARD FOR TASK 19 (from the reviewer's two ⚠️ items plus Minor 2; resolved by controller as future-task obligations, not present gaps — this diff is correct under these assumptions, so Task 19 must make them true and its review must verify all three): (a) originGuard/authenticate may be constructed ONLY in hosted mode — a nil Deps.Auth would panic at authmw.go:210 if authenticate were wired unconditionally in standalone, which would also break standalone equivalence; (b) originGuard must be chained BEFORE authenticate, as the code comments assert; (c) Deps.LoginSessionTTL must be wired to a NON-ZERO value from config — setSessionCookie (authctx.go:103) computes MaxAge as int(TTL.Seconds()), and a zero TTL emits MaxAge: 0, which per RFC 6265 means "delete immediately" rather than "session cookie", i.e. a wiring mistake that fails silently in the wrong direction.
Task 16: minor (deferred): clientIP reads X-Forwarded-For with Header.Get, which returns only the first field line and does not join values split across repeated header lines — irrelevant under the documented single-proxy model, worth a comment if a multi-proxy chain ever appears.
Task 16: complete (commits ce737be..beb27ff, review clean — approved, minors only)
Task 17: dispatched (base beb27ff, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-17-brief.md)
Task 17: implementer DONE, commit b5949cc. classify: the *auth.Error errors.As arm was ALREADY in place from Task 16 and verified against the status table (no duplicate added); the errors.Is(err, auth.ErrNotFound) case WAS missing from the not-found switch and was added, matching the brief's Step 1. Mutation ratios 2/2 (Retry-After suppression -> TestLoginReturnsRetryAfterOnLockout FAIL; removed auth.ErrNotFound case -> TestClassifyAuthNotFound FAIL). Declared deviations: test scaffolding lives in auth_test.go rather than router.go (route registration is Task 19's); helper named doAuth to avoid colliding with api_test.go's existing 33-call-site do(); cookie-clear assertions read raw Set-Cookie text per the authmw_test.go pattern; two extra classify unit tests because the HTTP path to the new not-found arm is unreachable via the ordinary deletion cascade.
Task 17: SPEC-vs-PLAN CONFLICT on logout, ruled by controller. The implementer flagged that logout must not sit behind the blanket authenticate middleware or a missing/stale cookie 401s instead of the brief's contracted idempotent 204 (TestLogoutIsIdempotent). Checking the binding spec: api-contracts.md:113-118 says "Requires a session. Hosted only." and documents only a 204. Ruling: logout is EXEMPT from authenticate (register it via publicRoute) and answers 204 whether or not a valid session cookie arrives. Reasoning: api-contracts.md's "Requires a session" describes the happy path and specifies no 401 response for logout at all, while the plan's explicit TestLogoutIsIdempotent is the more specific instruction; and gating logout on authenticate is actively user-hostile — a user holding an expired or unknown cookie could never clear it, which is the one thing logout exists to do. Cost if wrong: a logout with no session answers 204 instead of 401. No security impact — logout with no session deletes no row, and the Set-Cookie clear is the same either way. TASK 19 must wire logout outside authenticate; TASK 28 should clarify api-contracts.md:115 to say the session is optional and the response is always 204.
Task 17: task reviewer dispatched (model sonnet, range beb27ff..b5949cc)
Task 17: review — spec OK, quality approved, 0 Critical/0 Important, 2 Minor. auth.go matches the brief's Step 4 reference code; errors.go:602 adds exactly one errors.Is(err, auth.ErrNotFound) arm as a third alternative in the existing not-found case with no reordering, and there is exactly ONE *auth.Error arm (errors.go:33-35, pre-existing from Task 16) — no duplicate. Security checks all hold: every mutating handler derives the user only from scopeFrom(r).UserID() (auth.go:158,182,196) and neither request-body struct carries a user-identifying field; clientIP(r) (not RemoteAddr) is threaded into Register and Login (auth.go:103,121); logout/changePassword source the token via sessionTokenFrom(r) (auth.go:150) and tokenHashFrom(r) (auth.go:182) rather than re-hashing the cookie; no password/token/hash appears in any response struct (auth.go:34-71) or log line (writeDomainError logs err.Error() only at 5xx, and every auth code here is 401/409/422/429). Login oracle intact — the handler forwards the domain error unchanged with no existence branch.
Task 17: logout ruling verified at the handler level — the reviewer traced Service.Logout (service.go:218-237) and confirmed a "" token is a no-op returning nil, so the handler is genuinely idempotent with no nil-deref when reached without a session, and its only log line fires on a real store error and carries no token. ChangePassword's keep = tokenHashFrom(r) reaches Store.DeleteOtherLoginSessions(ctx, userID, keep) (service.go:294), revoking other sessions while preserving the caller's, per FR-2.6 and api-contracts.md:170-171. writeAuthError (auth.go:139-146) is reused by register/login/changePassword/deleteAccount, so Retry-After accompanies a lockout from any of them — broader than the brief's register/login-only framing and correct, since the domain can raise CodeAccountLocked from more than one entry point.
Task 17: minor (deferred): the test-only router wraps me/password/DELETE me in s.authenticate individually instead of a shared protected sub-mux (Task 19 replaces it anyway); the two new classify tests exercise classify directly rather than over HTTP because the delete-account cascade removes the row before an HTTP caller could observe auth.ErrNotFound — self-disclosed and justified, noted only as a departure from the brief's Step 2 list.
Task 17: complete (commits beb27ff..b5949cc, review clean — approved, minors only)
Task 18: dispatched (base b5949cc, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-18-brief.md) — carries the BINDING empty-token VALIDATION_ERROR 422 obligation from the Task 15 ruling.
Task 18: implementer DONE_WITH_CONCERNS, commit 789543a. Empty-token 422 satisfied via the brief's own validationError helper (settings_providers.go:74, called at :107) with test TestCreateValidatesTheRequestShape/missing_token. Mutation ratios: ErrNotFound arm 1/1, create validate-default 1/1, empty-token pre-check 0/1 (the pre-check is redundant with writeProviderSettingsError's catch-all arm, which also yields 422 — the behaviour is protected but not by that mechanism in isolation; the explicit check was kept per dispatch instruction).
Task 18: MY DISPATCH WAS WRONG and the implementer was right to correct it — I told it to reuse "an established input-error type that classify already maps" to VALIDATION_ERROR. No such mechanism exists in this repo. It used the brief's own validationError helper, which bypasses classify and writes the code directly, matching the execution-notes.md precedent. Accepted; no rework.
Task 18: flake claim NOT REPRODUCED — the implementer reported a pre-existing internal/auth timing flake in the full suite. Controller ran `go test -race -count=3 ./internal/auth/` (ok, 14.578s) and then a full `go test -race -count=1 ./...` across the module: entirely green, zero failures. Recording as unreproducible rather than pre-existing; the final whole-branch review should watch for it, since an intermittent timing test that only fails under full-suite load is still a real risk even when it passes on demand.
Task 18: CONTROLLER-FOUND CONCERN routed to the reviewer for independent judgement — writeProviderSettingsError (settings_providers.go:178-185) maps *auth.Error and auth.ErrNotFound to writeDomainError and sends EVERYTHING ELSE to validationError(err.Error()), i.e. 422 VALIDATION_ERROR with the raw error text as a client-visible detail. Its doc comment justifies this by asserting every plain error from auth's provider layer is "a fixed, value-free message", but CreateProvider/UpdateProvider also return unwrapped store errors (a DB failure) and HTTPVerifier errors (a provider outage is a plain non-ErrAuth error), neither of which is fixed or value-free. If that reading is right, a database fault or an upstream outage answers 422 with internal detail leaked to the client instead of a 5xx. Flagged for the reviewer to confirm or refute against the actual error paths.
Task 18: task reviewer dispatched (model sonnet, range b5949cc..789543a)
Task 18: review — spec OK, quality NEEDS FIXES, 1 CRITICAL, 0 Important, 2 Minor. My controller-found concern is CONFIRMED with full path evidence, and the doc comment's justifying claim is demonstrably false. The reviewer traced all three families that reach writeProviderSettingsError's catch-all as plain %w-wrapped errors: (1) DB faults — Store.CreateUserProvider returns "auth: begin create user provider: %w" / "auth: check provider slug: %w" / "auth: insert user provider: %w" / "auth: commit create user provider: %w" (store.go:233-256), none a *auth.Error; (2) provider outages — httpVerifier.Verify (verify.go:34-51) types ONLY errors.Is(err, provider.ErrAuth) as CodeProviderUnauthorized and returns everything else (unreachable host, 500, timeout) as plain "auth: verify provider credential: %w", reaching the catch-all from both CreateProvider (providers.go:56-58) and UpdateProvider (providers.go:129-141); (3) crypto faults — Sealer.Seal nonce failure (crypt.go:63-72) and Sealer.Open decrypt failure (crypt.go:75-86) both plain, reaching it from providers.go:67-68,136-138,146-148. So a database fault, an upstream outage, or a crypto failure answers 422 VALIDATION_ERROR with raw Go error text as a client-visible detail on a credentials endpoint. No token is exposed (TestNoResponseEverContainsAToken still valid), but the HTTP semantics are wrong and it is an information-disclosure surface.
Task 18: PATCH-with-slug returning 400/INVALID_REQUEST rather than api-contracts.md:308's literal 422/VALIDATION_ERROR is NOT a Task 18 finding — the reviewer traced it to execution-notes.md:40-42, where it is already recorded as binding project practice from an earlier session. No action.
Task 18: fix round 1/5 — SendMessage is unavailable in this harness (same as session 2), so a FRESH task-implementer was dispatched carrying the brief path, the report path, and the findings; the report file is the persistent memory.
Task 18: fix round 1 implementer DONE, commit 7637842 "fix(task-018): fail closed on unrecognised provider-settings errors". Took the preferred sentinel approach: added auth.ErrInvalidInput, wrapped the four genuine input-validation failures (ValidateSlug, NormalizeBaseURL x2, normalizeKind, CreateProvider's token check) with it, and INVERTED writeProviderSettingsError so errors.Is(err, auth.ErrInvalidInput) -> 422 and everything else -> writeDomainError/classify default (500, no leaked text). Dropped the now-redundant handler-level empty-token pre-check, having proved the check load-bearing at the DOMAIN level by mutation instead. Mutation ratios 6/6 (open-catch-all reversion 3/3, ValidateSlug unwrap 1/1, CreateProvider empty-token removal 1/1). Minor closed too: added TestPatchWithAnExplicitEmptyTokenKeepsTheStoredOne. Full suite green including internal/auth, no flake this run.
Task 18: scoped re-review dispatched (model sonnet, fix range 789543a..7637842)
Task 18: re-review — ALL FINDINGS ADDRESSED, no new Critical/Important breakage. Critical ADDRESSED at settings_providers.go:171-183: errors.Is(err, auth.ErrInvalidInput) -> 422, everything else -> writeDomainError, whose classify default (errors.go:70) returns a fixed 500/GIT_FAILURE/"An unexpected error occurred..." and never err.Error(); the text is logged only (errors.go:25). All three families re-traced in current code and each now has a test that uses a REAL fault, not a stub: store_fault runs a closed-handle store call, TestCreateVerifierOutageAnswers5xx asserts no "connection refused" leak, crypto_fault runs a real Sealer.Open on non-sealed ciphertext. 422 path intact — ValidateSlug (model.go:65), NormalizeBaseURL both branches (model.go:92,98), normalizeKind (providers.go:191) and CreateProvider's token check (providers.go:52) all wrap ErrInvalidInput at a single %w level so errors.Is sees through. PROVIDER_UNAUTHORIZED did not regress: a *auth.Error is a distinct type, not sentinel-wrapped, so it falls to classify's errors.As arm and keeps its own code. Minor ADDRESSED: TestPatchWithAnExplicitEmptyTokenKeepsTheStoredOne asserts tokenLast4/tokenSetAt unchanged AND re-resolves the registry so the stored ciphertext still decrypts — proves preservation, not just a 200.
Task 18: regression surface cleared — the reviewer grepped every existing caller of the four wrapped functions (providers.go:40,43,47,108,113, model.go:189) and confirmed all check only err != nil, never message text or type, so the %w wrapping changes nothing observable. No pre-existing test function gained assertions; the diff adds only new test functions. auth.ErrInvalidInput (errors.go:39) follows the existing ErrNotFound sentinel pattern in the same file, and classify has no competing ErrInvalidInput arm. Trailer on 7637842 confirmed.
Task 18: fix round 1/5 (2 addressed, 0 open; commits 789543a..7637842)
Task 18: complete (commits b5949cc..7637842, review clean after 1 fix round)

## HANDOFF at Task 18 (2026-09-11)

Session 3 ran Tasks 14-18 (five tasks plus one fix round), the same cadence at which sessions 1 and 2 handed off. Controller stopping at the context-handoff threshold on a clean boundary: the entire backend domain and handler layer is now complete and reviewed, and the next unit (Tasks 19-20, route wiring and app wiring) is where standalone equivalence finally becomes provable end to end.
Resume by: `/clear`, then `/execute-task task-005` from the worktree. Base for Task 19 is `7637842`. Briefs for Tasks 16-20 are already extracted in this workspace.

## Session 4 (2026-09-11) — resumes at Task 19
Task 19: dispatched (base 4b75106, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-19-brief.md) — carries the four BINDING items from the Task 16 and Task 17 rulings: (a) originGuard/authenticate constructed only in hosted mode; (b) originGuard chained before authenticate; (c) Deps.LoginSessionTTL wired non-zero (RFC 6265 MaxAge=0 hazard); (d) logout registered OUTSIDE authenticate, always 204.
Task 19: implementer DONE, commit 79e62cf "feat(api): scope the review handlers and register auth routes by mode". All five gates clean (go vet, go test -race ./..., integration-tagged, golangci-lint, CGO_ENABLED=0 build). Mutation ratios 3/3 (logout-public exemption, hosted Corrupted scope-gate, deleteReview visibility check), each a quoted assertion failure rather than a compile error. Binding items (a) (b) (d) implemented; (c) LoginSessionTTL left as a plain field with Task 20 supplying the value from config.
Task 19: brief conflict declared and resolved by the implementer — TestStandaloneRouterIsUnchanged cannot rely on a bare Deps{} zero value, because Mode then holds "" rather than config.ModeStandalone; the test sets Mode explicitly. Production wiring goes through config.LoadConfig, which defaults to standalone. Routed to the reviewer to judge whether the "" zero value is a latent production hazard.
Task 19: two deviations declared — TestLogoutIsPublicUnderTheRealRouter and TestCorruptSessionAnswers404InHostedMode, neither among the brief's 8, added because none of the 8 exercised the binding-requirement branches through the real NewRouter; the implementer reports verifying by mutation that those branches would otherwise be unreachable. Also made createReviewAs poll to completion to fix a t.TempDir cleanup race exposed by newHostedFixture not setting BuildContext. Routed to the reviewer.
Task 19: task reviewer dispatched (model sonnet, range 4b75106..79e62cf) — carries all four binding items plus both deviations for adjudication.
Task 19: review — spec OK, quality approved, 0 Critical/0 Important, 2 Minor. All four binding items PASS. (a) router.go:745-753 wraps handler = s.authenticate(handler) then s.originGuard(handler) strictly inside if d.Mode == config.ModeHosted; standalone gets the bare mux under withMiddleware only, so the closure that dereferences a nil Deps.Auth is never constructed. (b) authenticate is innermost and originGuard outermost, so a request meets originGuard first, matching authmw.go's comment. (d) logout answers 204 regardless of cookie validity. The 8 brief tests plus both added tests pass; the two t.Skips are genuinely gone and now run against the real NewRouter; classify still has exactly one *auth.Error errors.As arm (errors.go:34) and one auth.ErrNotFound errors.Is arm (errors.go:61), no duplicates; cmd/converge-cli/main.go has zero diff across the range; trailer present on 79e62cf.
Task 19: RULING — binding item (c) was built on a FALSE PREMISE and is hereby CORRECTED for Task 20. Task 16's carry-forward asserted that a zero LoginSessionTTL emits "MaxAge: 0" meaning "delete immediately" per RFC 6265. The reviewer refuted it and I verified independently against the Go stdlib: $GOROOT/src/net/http/cookie.go:36-38 documents "MaxAge=0 means no 'Max-Age' attribute specified. MaxAge<0 means delete cookie now, equivalently 'Max-Age: 0'. MaxAge>0 means Max-Age attribute present and given in seconds." So a zero TTL omits the attribute entirely and degrades to a browser-session cookie — milder than described, and NOT a silent failure in the delete direction. Task 16's Max-Age=0 clear-cookie test is unaffected: it asserts the wire output Go produces from MaxAge=-1. Task 19 leaving LoginSessionTTL as a plain field is therefore adequate, not a gap. TASK 20 must still wire it non-zero from config (LOGIN_SESSION_TTL_HOURS defaults to 720 and is tested), but the failure mode if it slips is a session-lived cookie, not an unusable one. Cost if wrong: a mis-wired TTL logs users out when the browser closes instead of at 30 days — a UX regression, not a security or availability failure.
Task 19: RULING on binding item (d)'s mechanism — satisfied behaviourally by a route exemption rather than structurally. Logout is registered on the same mux inside the hosted branch (router.go:719) and is still nominally wrapped by authenticate; the exemption lives at authmw.go:24, where publicRoute now includes "/api/auth/logout", so authenticate short-circuits and never calls Auth.Authenticate for it. Accepted: the ruling's intent was the observable behaviour (a 401 must never block a logout), and TestLogoutIsPublicUnderTheRealRouter proves it, failing with a 401 when the exemption is mutated away. Cost if wrong: none in shipped behaviour; the risk is that a future edit to authenticate's own body could regress logout in a way a "structurally outside" wiring could not — the mutation-backed test is the guard against that.
Task 19: both deviations accepted. The two extra tests close real coverage gaps — neither of the brief's 8 exercised the binding-requirement branches through the real NewRouter, and the branches were mutation-proven reachable-only-through them. TestStandaloneRouterIsUnchanged's explicit Mode is justified and the "" zero value is NOT a production hazard: the reviewer traced NewRouter and any non-ModeHosted value behaves identically to standalone, and production Deps.Mode always originates from config.LoadConfig, which defaults to ModeStandalone. Test-fixture concern only.
Task 19: minor (deferred): setSessionCookie (authctx.go) has no defensive floor on LoginSessionTTL <= 0 — a cheap `if ttl <= 0 { ttl = default }` would be insurance against a future mis-wiring, though config defaults safely today. Also the plan/brief wording "logout registered OUTSIDE authenticate" does not match the implemented publicRoute exemption; fold the wording fix into Task 28 alongside the other doc corrections.
Task 19: complete (commits 4b75106..79e62cf, review clean — approved, minors only)
Task 20: dispatched (base 79e62cf, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-20-brief.md) — carries the corrected TTL obligation and the standalone-equivalence proof burden.
Task 20: implementer DONE_WITH_CONCERNS, commit b769da1 "feat(app): wire hosted mode — database, auth service, and per-user resolver". All five gates green including cmd/converge, cmd/converge-cli and internal/app, plus a manual standalone/hosted smoke test. Mutation ratios 2/2 (hosted-gate-always-on caught by TestStandaloneCreatesNoDatabaseFile; suppressed-WARN caught by TestHostedWarnsAboutIgnoredProviderVariables), both mutated outside the worktree in /tmp and reverted with git status confirmed clean. One lint-only deviation from the brief's sample code (ST1023 on an explicit interface-type var declaration).
Task 20: PLAN-TEXT DEFECT found by the implementer and RULED by the controller. Step 4 prescribes `go list -deps ./cmd/converge-cli | grep auth|db` returning no output, but that is unsatisfiable given Step 3's own prescribed code: cmd/converge-cli shares app.New (verified at cmd/converge-cli/main.go:74, `application, err := newApp(ctx, os.Environ())` with `var newApp = app.New` at :31), so Go's STATIC import graph pulls internal/auth and internal/db into the CLI's dependency set the moment app.go imports them, regardless of runtime mode. Ruling: the Step 4 check is the wrong instrument, not a real requirement — the property the plan actually wants is a RUNTIME one (the CLI opens no database and constructs no auth machinery), and satisfying the literal link-time check would demand an out-of-scope architectural split of app.New. The implementer was right to follow Step 3 verbatim and leave the check failing. Cost if wrong: if the plan genuinely intended link-time separation, the CLI binary carries the sqlite driver and auth code it never runs, inflating binary size — no correctness or security impact. Fold the Step 4 correction into Task 28.
Task 20: CONTROLLER-FOUND CONCERN routed to the reviewer for independent judgement and classification (not pre-judged) — because cmd/converge-cli/main.go:74 calls app.New(ctx, os.Environ()), a CONVERGE_MODE=hosted value present in the CLI's environment would make converge-cli open the database, run migrations, and build the auth machinery it never uses. This is reachable in practice: exec'ing the CLI inside the hosted container inherits exactly that environment. The reviewer must confirm or refute against the actual app.New gating, judge whether a second process opening the same SQLite file under MaxOpenConns(1) is harmful or merely wasteful, and classify. The CLI's own call sites remain permanently identity.Standalone(), so any auth machinery it built would be dead weight.
Task 20: task reviewer dispatched (model sonnet, range 79e62cf..b769da1) — carries the standalone-equivalence proof burden for the whole branch, the plan-text ruling, and the controller-found CLI concern.
Task 20: review — spec OK, quality Approved, 0 Critical, 1 IMPORTANT, 0 Minor. All six verification items PASS. Standalone gating verified at app.go:296-329 with App.Auth/App.DB/App.ProviderResolver set only inside the hosted branch (:391-395); TestStandaloneCreatesNoDatabaseFile (app_test.go:373-390) is a genuine absence assertion — os.Stat returns os.ErrNotExist even with CONVERGE_DATABASE_PATH explicitly set, and it checks the default /data/converge.db was never touched. LoginSessionTTL wired from application.Config.LoginSessionTTL with config.go:274 defaulting to 720h. Close guards the nil DB at app.go:75-79 under errors.Join. Sweeper started exactly once — router.go:90-106 holds the single goroutine and Task 20 supplies only the AuthSweep value. Migration order is strictly db.Open -> db.Migrate -> auth.NewStore -> NewProviderResolver -> ServiceDeps, and a Migrate failure closes the handle and returns before any auth machinery is built. Secret hygiene verified live: TestHostedWarnsAboutIgnoredProviderVariables asserts the token value is absent from captured stderr, and config.Secrets() (config.go:104-119) feeds SecretKey.Reveal() into NewLogger's redacting handler covering every new log line. No pre-existing test function gained assertions. Trailer on b769da1 confirmed.
Task 20: my controller-found concern (a) CONFIRMED and classified IMPORTANT by the reviewer, against code rather than the report. cmd/converge-cli/main.go:31,74 plus permanently-Standalone call sites at :82,114,133. Corruption is NOT the risk — db.Open (db.go:52-73) uses WAL plus busy_timeout(5000) with MaxOpenConns(1) per process, and WAL is built for multi-process single-file access. The real harms are: (i) the CLI would require CONVERGE_SECRET_KEY and DB write access it never needed, failing outright where CONVERGE_MODE=hosted leaked in without the key; (ii) with CONVERGE_DATABASE_PATH unset it would open the PRODUCTION database at the default /data/converge.db inside the hosted container, taking an extra connection and running a migration check against the live DB. It violates design §11's explicit contract that converge-cli never opens the database.
Task 20: RULING — entering fix round 1 rather than deferring, despite the reviewer marking it Approved-with-fast-follow. An Important finding enters the loop by the skill's rule, and the fix is one verified line against a seam that is cheapest to close now, while Task 20's wiring is the live context. The reviewer verified the mechanism: config.Load builds vars from a map populated by iterating env IN ORDER (config.go:122-127), so a later duplicate key wins, making append(os.Environ(), "CONVERGE_MODE=standalone") at cmd/converge-cli/main.go:74 force standalone unconditionally. This overrides my own Task 19/20 dispatch instruction that cmd/converge-cli/main.go stay untouched: that instruction existed to protect the CLI's standalone nature, and this change ENFORCES it rather than regressing it. Cost if wrong: a one-line change plus one test in a file three prior tasks deliberately left alone; if the append mechanism were misread the CLI would still run, because CONVERGE_MODE=standalone is also the default.
Task 20: reviewer independently confirmed plan-text defect (b) by running the check — go list -deps ./cmd/converge-cli greps both internal/auth and internal/db, structurally forced once cmd/converge-cli imports internal/app. Concurs with my ruling that the link-time check is the wrong instrument and that TestStandaloneCreatesNoDatabaseFile is the stronger substitute, and that genuine link-time separation would require splitting app.New into distinct standalone/hosted constructors — outside this task's scope. Task 28 correction stands.
Task 20: fix round 1/5 — SendMessage unavailable in this harness (same as sessions 2 and 3, re-confirmed via ToolSearch), so a FRESH task-implementer was dispatched carrying the brief path, the report path, and the finding; the report file is the persistent memory, appended to rather than overwritten.
Task 20: fix round 1 implementer DONE, commit fc657d4 "fix(cmd/converge-cli): pin the CLI to standalone regardless of inherited CONVERGE_MODE". Took the prescribed append(os.Environ(), "CONVERGE_MODE=standalone") approach. New test TestRunStaysStandaloneWhenHostedModeLeaksIntoEnvironment (cmd/converge-cli/main_test.go), mutation ratio 1/1 — reverting the fix produces the assertion failure `CONVERGE_DATABASE_PATH exists after a hosted-leaked run: err=<nil>`, restoring it passes. All gates clean (go test -race ./..., go vet, golangci-lint 0 issues, CGO_ENABLED=0 build). No new concerns. Carried-over note, explicitly NOT part of this finding and unchanged by the fix: go list -deps ./cmd/converge-cli still shows transitive internal/auth and internal/db via app.App struct fields — that is the plan-text defect (b) already ruled on.
Task 20: scoped re-review dispatched (model haiku, fix range b769da1..fc657d4) — instructed to independently verify the last-duplicate-wins mechanism in config.Load rather than trust it, since an inert fix would look identical to a correct one.
Task 20: re-review — FINDING ADDRESSED, no new breakage. The last-duplicate-wins mechanism was independently verified against the actual source (config.go:122-127 iterates env in order assigning vars[k] = v, so a later entry overwrites an earlier one) rather than taken on trust — the fix is genuinely live, not inert. cmd/converge-cli/main.go:80 now appends "CONVERGE_MODE=standalone". TestRunStaysStandaloneWhenHostedModeLeaksIntoEnvironment (main_test.go:156-195) asserts absence via errors.Is(err, os.ErrNotExist) and is discriminating: the re-reviewer re-ran the mutation itself and got the database created when the fix is reverted. Notable test-design point confirmed — the test deliberately supplies a valid random CONVERGE_SECRET_KEY so auth.NewSealer succeeds and execution actually REACHES db.Open; without it the test would pass whether or not the fix were present. Diff is 2 files, one seam in main.go plus a wholly new test function; no pre-existing test gained assertions; pre-existing CLI tests all pass unchanged; trailer on fc657d4 confirmed.
Task 20: fix round 1/5 (1 addressed, 0 open; commits b769da1..fc657d4)
Task 20: complete (commits 79e62cf..fc657d4, review clean after 1 fix round)

=== BACKEND COMPLETE (Tasks 1-20). Tasks 21-25 are frontend, 26-29 are cross-cutting tests, docs, and the pre-PR review. ===
Task 21: dispatched (base fc657d4, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-21-brief.md). First frontend task — data layer only (client.ts, model types, Zod schemas, service objects, React Query hooks); the screens are Tasks 22-25. Dispatch carries the three agreed frontend-dev-guidelines deviations that are project law (Vitest not Jest, thin fetch wrapper not a caching client, plain service objects not a BaseService class) so the implementer does not "fix" them toward the skill default, plus the verified backend contract facts: HttpOnly converge_session cookie means credentials:"include" and no token for JS to manage; logout ALWAYS 204 and never 401; the ten error codes with statuses; Retry-After on ACCOUNT_LOCKED 429; provider responses carry only tokenLast4/tokenSetAt and a PATCH with omitted/empty token keeps the stored one; and in standalone the auth routes are NOT REGISTERED so they 404 rather than 401 — hooks must not assume they exist. Flagged setUnauthorizedHandler's contract as load-bearing for Task 22's AuthProvider.
Task 21: implementer DONE, commit ea428f8 "feat(frontend): add the auth API layer and a 401 interceptor". Frontend gates all clean (npm test 271/271, lint, format:check, build). Mutation ratio 1/1 — flipping response.status === 401 to 402 in a scratch copy outside the worktree was caught by client.test.ts's "invokes the unauthorized handler on 401 and still throws"; scratch deleted and the worktree confirmed unmutated.
Task 21: FLAKE WATCH ITEM STRENGTHENED — the implementer independently reported the same internal/auth timing flake under full-suite load that Task 18's implementer reported, again passing on an isolated re-run. Session 3 recorded it as UNREPRODUCIBLE after go test -race -count=3 ./internal/auth/ and a full green module run. This is now a SECOND INDEPENDENT SIGHTING from a different agent on a different task, which materially raises the odds it is real rather than noise. It is not a Task 21 defect — nothing in this commit touches Go. Carrying it as a named obligation for the final whole-branch review and for Task 27 (the hosted integration test): the flake must be either reproduced and fixed or positively ruled out before the PR, not left as folklore. Cost if ignored: an intermittently red suite lands on main and erodes trust in every later run.
Task 21: brief conflict declared and resolved by the implementer — the brief's Interfaces block types UserProvider/CurrentUser FLAT while its prose says "Resource-compatible ... following provider.ts style" (which wraps in Resource<>). The implementer followed the flat Interfaces block verbatim, on the grounds that it is the exact-value contract Tasks 22-25 will consume, and used Resource<> only internally in the service layer before flattening — the same shape as the brief's own authService.toUser. Routed to the reviewer to confirm the flat shape is what Tasks 22-25 should consume.
Task 21: two sample-code deviations declared — UserResource.providerCount typed optional because the brief's literal sample type would not compile against the actual wire payload, and z.literal's message passed as a plain string (Zod v4 plus existing codebase convention) rather than an object. Both routed to the reviewer.
Task 21: task reviewer dispatched (model sonnet, range fc657d4..ea428f8)
Task 21: review — spec OK, quality Approved, 0 Critical, 1 IMPORTANT, 0 Minor. All six verification items PASS. credentials:"include" is set once inside request() and every verb helper (apiGet/apiPost/apiDelete/apiPatch/apiPostNoContent/apiDeleteWithBody) routes through it. The seam rejects after firing (throw runs unconditionally), no-ops when unregistered, and is leak-guarded by beforeEach(setUnauthorizedHandler(null)). Nothing touches localStorage/sessionStorage. ["providers"] invalidation is onSuccess-only on all three mutations, and prefix matching also catches providerKeys.lists(). No pre-existing client.test.ts assertion changed — only an import line, a module-level beforeEach, and five appended it() blocks. Zod schemas cross-checked byte-for-byte against the backend: username regex identical to usernameRe, slug regex identical to slugRe, password length-only 8-1024 matching MinPasswordLen/MaxPasswordLen with no composition rule on either side, and baseURL's gitlab-required/github-defaults refine matching NormalizeBaseURL.
Task 21: the IMPORTANT finding — client.ts:35-38 fires onUnauthorized?.() on ANY response.status === 401 with no exemption, so a 401 meaning "the credentials you just supplied are wrong" trips the same global session-expiry channel as a genuinely dead cookie. The reviewer judged the blast radius low on the grounds that the user is already on /login. I CHECKED THAT PREMISE AND IT IS WRONG, which changes the severity: grep of internal/auth/service.go shows CodeInvalidCredentials is returned from THREE sites, not one — :191 (login), :282 ChangePassword "The current password is incorrect.", and :316 DeleteAccount's password confirmation. The latter two are reachable by a FULLY LOGGED-IN user with a perfectly valid session who simply mistypes their current password on the Account Settings screen (Task 25). That user would be spuriously logged out and have their query cache cleared. That is a real defect, not a redundant navigate.
Task 21: RULING — fix now in Task 21's seam rather than carrying it into Task 22's brief as the reviewer suggested. Two reasons: the defect is in the seam's own contract, and Tasks 22 and 25 both build on that contract and cannot change it cheaply once they do. Prescribed discriminator is the error CODE, not a URL allowlist: fire onUnauthorized only for UNAUTHENTICATED (the session is invalid or expired) and never for INVALID_CREDENTIALS (the supplied credentials were rejected). The backend already separates these precisely at errors.go:65, so the frontend can key off the code it already parses; a URL allowlist would be the fragile version of the same fix. Cost if wrong: if some protected route ever returned a bare 401 with no UNAUTHENTICATED code, the global handler would stop firing for it and a dead session would go unnoticed until the next navigation — so the fix must verify the middleware's 401 actually carries the code.
Task 21: fix round 1/5 — fresh task-implementer dispatched (SendMessage unavailable), carrying the brief path, the report path, and the finding.
Task 21: fix round 1 implementer DONE, commit a872dea "fix(task-021): discriminate UNAUTHENTICATED from INVALID_CREDENTIALS on 401". Discriminates on error.code as prescribed, NOT a URL allowlist. Fail-safe implemented as instructed: fires on UNAUTHENTICATED and on any unrecognised/absent code, does not fire on INVALID_CREDENTIALS. Implementer reports verifying against service.go, authmw.go and errors.go that every dead-session 401 carries UNAUTHENTICATED. Mutation ratios: guard-literal 1/1 (the INVALID_CREDENTIALS test), always-suppress 3/3 (the three "fires" tests); both mutated in scratch copies outside the worktree with git status and grep confirming no residue. client.test.ts 13/13, full frontend suite 274/274, lint/format:check/build clean, and the backend gate clean with no flake on this run.
Task 21: scoped re-review dispatched (model haiku, fix range ea428f8..a872dea) — instructed to confirm the fail-safe and the UNAUTHENTICATED-on-every-dead-session claim hold TOGETHER, since the fail-safe is what covers the case where the claim is false.
Task 21: re-review — FINDING ADDRESSED, no new breakage. client.ts:57 now guards on `response.status === 401 && error.code !== "INVALID_CREDENTIALS"`, discriminating on the code parsed from the response body at :56, with no URL allowlist anywhere. Fail-safe confirmed by mechanism, not just by test: toApiError returns code "UNKNOWN" for a non-JSON 401 body (client.ts:19) — an HTML error page from a proxy, say — and "UNKNOWN" !== "INVALID_CREDENTIALS", so the handler fires and a dead session is never silently swallowed. Line 60 throws unconditionally, so every 401 still rejects whether or not the handler fired. Three new tests cover all three directions (UNAUTHENTICATED fires; INVALID_CREDENTIALS against the REAL /api/auth/password endpoint does not; an opaque HTML 401 fires). The doc comment at :22-39 was rewritten to state the contract as "the session is gone — never merely 'a request returned 401'", which is what Task 22's AuthProvider author will read. Leak guard intact at :16; no pre-existing test gained assertions; trailer on a872dea confirmed.
Task 21: backend assumption independently re-verified by the re-reviewer — every path in Authenticate() (service.go:244-272) returns CodeUnauthenticated for missing, unknown, and expired tokens, while CodeInvalidCredentials appears ONLY at Login:191, ChangePassword:282 and DeleteAccount:316, all from password verification. Both map to 401 at errors.go:65, and classify writes the code to the wire without dropping it. So the discriminator and the fail-safe hold together.
Task 21: minor (deferred): mild test redundancy — the original task-21 test at client.test.ts:98-110 and the fix-round test at :141-153 both cover the UNAUTHENTICATED-fires direction against different endpoints. Harmless; fold into any later test-hygiene pass.
Task 21: fix round 1/5 (1 addressed, 0 open; commits ea428f8..a872dea)
Task 21: complete (commits fc657d4..a872dea, review clean after 1 fix round)

## HANDOFF at Task 21 (2026-09-11)
Session 4 ran Tasks 19-21 plus two fix rounds. Controller stopping at the context-handoff threshold on a clean boundary: the entire backend (Tasks 1-20) is complete and reviewed, and the frontend data layer (Task 21) is done, so the next unit is Tasks 22-25, the four screens, which consume the seam and hooks Task 21 just finished.
Resume by: `/clear`, then `/execute-task task-005` from the worktree. Base for Task 22 is `a872dea`. Briefs through Task 21 are extracted in this workspace; Task 22's must be extracted with scripts/task-brief.

## SESSION 5 (2026-09-11) — resuming at Task 22
Task 22: dispatched (base a872dea, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-22-brief.md). First screen-level frontend task — mode gate, AuthProvider, route guard, account menu; consumes Task 21's data layer. Dispatch carries the five load-bearing facts the brief cannot know: (1) the narrowed setUnauthorizedHandler contract at client.ts:22-39 ("the session is gone", fires on UNAUTHENTICATED and unrecognised codes, never on INVALID_CREDENTIALS) with an explicit prohibition on re-adding a blanket 401 reaction, since wrong-current-password 401s are reachable by a logged-in user on Task 25's screen; (2) Task 21's model types are FLAT, not Resource<>-wrapped; (3) HttpOnly converge_session cookie means no token for JS and no localStorage/sessionStorage; (4) standalone does not register the auth routes at all, so they 404 rather than 401 and the mode gate is what prevents the calls; (5) logout always 204. Plus the three project-law frontend-dev-guidelines deviations and the no-subagents contract. Brief-conflict rule given up front: follow the Interfaces block verbatim over the prose and declare the conflict, matching the ruling made in Task 21.
Task 22: implementer DONE_WITH_CONCERNS, commit 1b4b7fe "feat(frontend): add the mode gate, 401 interceptor, and route guard". All frontend gates clean (292 tests, lint, format:check, build) and the full backend gate clean too, with NO auth flake on this run (third clean full-suite run; the flake remains a Task 27 / final-review obligation, not yet ruled out). Mutation ratios: AuthProvider no-op guard 1/1, RequireAuth gate condition 2/2, both mutated in /tmp scratch outside the worktree and reverted with the worktree confirmed clean.
Task 22: four declared deviations from the brief, all routed to the reviewer — (1) the brief's AuthProvider sample mutates a ref during render, which fails the react-hooks/refs lint rule; moved into its own effect. (2) AccountMenu's Interfaces-block return type ReactElement contradicts the brief's own required `return null`; resolved by omitting the annotation, matching codebase convention. (3) the brief's routes.tsx sample imports four pages Tasks 23-25 have not built yet; the implementer created minimal real placeholder pages. (4) App.test.tsx's module-level singleton QueryClient with staleTime Infinity leaked cache across tests once ModeGate existed; fixed with a freshApp() helper (vi.resetModules + dynamic import) that touched every existing render(<App />) call site without changing any assertion.
Task 22: RULING on deviation 3 (the placeholder pages) — ACCEPT, with a carried obligation. The brief's own routes.tsx sample requires those four imports and its tests 11-12 are unsatisfiable without them, so the task as written cannot compile in isolation; refusing the placeholders would have meant failing the brief. The cost if wrong is that a placeholder is mistaken for a finished screen, so the mitigation is explicit: Tasks 23, 24 and 25 dispatches MUST state that they REPLACE the placeholder file for their screen rather than adding a parallel component, and the final whole-branch review must confirm no placeholder survived. Recorded here so the obligation outlives this session.
Task 22: RULING on deviation 4 (App.test.tsx call sites) — ACCEPT. Global Constraint 18 forbids adding new assertions to pre-existing test functions while changing the code under them; it does not forbid changing how those tests construct their subject. A leaked module-level QueryClient is a real test-isolation defect that ModeGate merely exposed. Reviewer instructed to verify the no-assertion-changed claim byte-for-byte rather than take it on report. Cost if wrong: a pre-existing test silently stops asserting what it used to, and standalone equivalence would no longer be proven by a green suite.
Task 22: task reviewer dispatched (model sonnet, range a872dea..1b4b7fe)
Task 22: review — spec ✅, quality Approved, 0 Critical, 0 Important, 2 Minor, 0 "cannot verify" items. All four declared deviations adjudicated as correct on their merits, not merely as declared. (1) The ref-via-effect AuthProvider has NO staleness window: the ref-update effect is declared before the handler-registration effect so both run in declaration order on mount, and on later navigations only the ref effect re-fires because the handler effect's deps [navigate, queryClient] are stable. (2) The omitted AccountMenu return annotation is harmless — AppShell's `right` prop is ReactNode, which accepts the inferred ReactElement | null. (3) The placeholders are bare PageHeaders with doc comments naming the replacing task, no forms, handlers or state — none silently satisfies Task 23-25 scope. (4) THE LOAD-BEARING ONE: the reviewer diffed all three pre-existing App.test.tsx bodies line by line against a872dea and confirmed every expect() call is character-for-character unchanged; the only edits are obtaining App via await freshApp() and adding an await findByRole to let ModeGate's now-async mode resolution settle. Standalone equivalence is still soundly proven by that suite, so my acceptance ruling holds.
Task 22: two independent confirmations worth carrying — the diff introduces ZERO blanket `status === 401` checks (AuthProvider only calls setUnauthorizedHandler and never inspects a response), and no localStorage/sessionStorage anywhere. Also verified from backend source rather than assumption: /api/auth/mode IS registered in standalone (router.go:113, proven unauthenticated by auth_test.go:365-366), so ModeGate's one fetch per page load is sanctioned; /api/auth/me is never requested in standalone, asserted in both ModeGate.test.tsx and App.test.tsx. Trailer confirmed verbatim on 1b4b7fe.
Task 22: minor (deferred): the task-22 report calls ErrorBanner's prop `message` while the code correctly uses the real prop `title` — report-only imprecision, no code defect.
Task 22: minor (deferred): mutation coverage is non-exhaustive for the ModeGate/AccountMenu branches (self-disclosed); each branch still has its own mutually-exclusive assertion. Fold into any later test-hygiene pass.
Task 22: complete (commits a872dea..1b4b7fe, review clean, 0 fix rounds)
Task 23: dispatched (base 1b4b7fe, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-23-brief.md). Login and register pages. Dispatch carries the Task 22 placeholder obligation EXPLICITLY — it must REPLACE pages/LoginPage.tsx and pages/RegisterPage.tsx in place, not add a parallel component beside an inert shell, and must remove the placeholder doc comment — plus the narrowed 401 seam contract with its direct consequence for this screen (a failed login does NOT trip the global handler and must render inline), the HttpOnly-cookie/no-storage rule, the flat model types, the five error codes it may surface with the length-only 8-1024 password rule and an instruction not to invent a composition rule, ErrorBanner's real prop name `title` (correcting the task-22 report's imprecision), the three project-law skill deviations, and the freshApp() test pattern.
Task 23: implementer DONE, commit 41a7892 "feat(frontend): add the login and register pages". Frontend gates clean (39 files / 310 tests, lint, format:check, build) and the full backend gate clean with no auth flake (fourth consecutive clean full-suite run). Mutation ratios: safeNext's open-redirect `//` guard 1/1 (quoted assertion `expected '//evil.test' to be '/'`), applyServerError's field-mapping fallback 3/3 (caught by RegisterPage field-mapping tests via aria-invalid mismatch). Scratch mutations reverted, git status clean.
Task 23: two declared deviations. (a) TWO PRE-EXISTING routes.test.tsx assertions were changed, getByText("Log in") -> getByRole("heading", {name:"Log in"}), because the real submit button made the old query ambiguous once the page became real. (b) aria-invalid added to inputs, absent from the brief's sample but claimed necessary for the brief's own specified test assertions and the shadcn Input styling contract.
Task 23: RULING on deviation (a) — PROVISIONALLY ACCEPT, pending the reviewer's byte-for-byte check, which I have made an explicit and load-bearing review item rather than accepting on report. Reasoning: Global Constraint 18 exists so that a green pre-existing suite still means what it appears to mean. getByRole("heading", {name}) is strictly NARROWER than getByText — it proves everything the old assertion proved plus that the match is the heading — so if the diff really is just those two lines, the suite proves MORE, not less, and the constraint's purpose is served rather than evaded. The ambiguity was also genuinely forced: a page that now contains both a "Log in" heading and a "Log in" submit button makes getByText throw on multiple matches, so the old query could not survive any correct implementation of this task. Cost if wrong: if the reviewer finds other pre-existing assertions were touched, or that the new query is weaker in some case I have not seen, then standalone equivalence is no longer proven by that suite and this enters the fix loop.
Task 23: task reviewer dispatched (model sonnet, range 1b4b7fe..41a7892) with three named adjudication items — the pre-existing-assertion change above; an ADVERSARIAL audit of safeNext's open-redirect guard against a named vector list (//evil.test, /\evil.test, https://evil.test, http:/\/\evil.test, scheme-relative with leading whitespace or control chars, and a non-string value), since a post-login `next` escaping to an attacker origin is a real vulnerability and the implementer only mutation-tested the `//` case; and whether the aria-invalid tests assert user-visible behaviour or an implementation detail the implementer chose.
Task 23: review — spec ✅, quality Approved, 0 Critical, 1 IMPORTANT, 0 Minor. All 12 brief scenarios present as distinct it() blocks; wire-contract codes all match with a test each; placeholders confirmed REPLACED IN PLACE via `git show 1b4b7fe:...` with no parallel component and no leftover doc comment (the Task 22 placeholder obligation is discharged for these two screens); password schema length-only 8-1024 with no invented composition rule; no localStorage/sessionStorage; no blanket 401 handling introduced, and INVALID_CREDENTIALS renders inline via errors.root and never trips the global handler; trailer confirmed on 41a7892.
Task 23: deviation (a) CONFIRMED CORRECT, my provisional ruling upheld. The reviewer diffed routes.test.tsx directly: exactly 2 lines changed, nothing else in the file, and the untouched "Create an account" assertion does NOT collide because the register button reads "Create account". getByRole("heading",{name}) is strictly narrower — PageHeader renders the title in an <h1> (PageHeader.tsx:12) — while getByText matched any element including the new submit Button, which genuinely collides. Ambiguity fix, not a weakening.
Task 23: deviation (b) CONFIRMED CORRECT. input.tsx:10 carries live aria-invalid:border-destructive / aria-invalid:ring-destructive/20 Tailwind variants, so without the attribute the shadcn error styling never activates. The assertions target screen-reader semantics plus visual error state, i.e. user-visible behaviour, not an implementation detail.
Task 23: THE IMPORTANT FINDING — the adversarial safeNext audit I ordered paid off. The guard `!raw || !raw.startsWith("/") || raw.startsWith("//")` correctly rejects //evil.test, https://evil.test, http:/\/\evil.test and whitespace/control-prefixed variants, but does NOT reject `/\evil.test`. The reviewer verified with the actual WHATWG URL parser that new URL("/\\evil.test", base).host === "evil.test" === new URL("//evil.test", base).host — browsers treat \ as / for special schemes — so the literal "//" prefix check misses the backslash form entirely. The prior implementer's mutation table only ever exercised the "//" branch, so no test would have caught it.
Task 23: RULING — fix now, in Task 23, rather than deferring. The brief's sample code mandated the vulnerable guard verbatim, so this is a finding against plan text and mine to decide; the plan is the argument but the spec's security intent is the authority, and a helper whose own doc comment claims open-redirect safety while not providing it is a defect no matter what the sample said. Decisive factor: TASK 25 IS SLATED TO REUSE safeNext, so leaving it would let a second screen build on the hole. Prescribed shape is additive — treat a backslash ANYWHERE in the raw value as disqualifying, keep the three existing checks untouched — and I explicitly forbade the new URL(...)/.origin alternative because it drags window.location into a pure helper and is harder to test. Cost if wrong: rejecting `\` outright could refuse some legitimate next path containing an encoded backslash; I judge that no legitimate internal path needs one, and the failure mode is a harmless fallback to "/" rather than a redirect.
Task 23: fix round 1/5 — fresh task-implementer dispatched (SendMessage re-confirmed unavailable in this harness via ToolSearch), carrying the brief path, the report path, the finding, the prescribed fix shape, four required regression cases (/\evil.test, /\/evil.test, /\\evil.test, plus a positive case), and an explicit DO-NOT-TOUCH list for the two review conclusions that were adjudicated correct rather than flagged.
Task 23: fix round 1 implementer DONE, commit 13bea39 "fix(task-023): reject backslash in safeNext to close open-redirect gap". formErrors.test.ts 9/9, full frontend gate clean (314/314 tests, lint, format:check, build), backend gate clean. Mutation ratio 3/3 — the new regression tests killed the reverted-guard mutation with quoted assertion `expected '/\evil.test' to be '/'`; scratch at /tmp deleted and git status confirmed no residue before commit. Implementer left routes.test.tsx and aria-invalid untouched per the ruling, and reports no concerns.
Task 23: note on an incidental — the implementer had to restore a dist/.gitkeep touched by `npm run build` before the backend gate was clean. Worth remembering for later tasks that run the frontend build: the build writes into apps/backend/internal/ui/dist and can dirty the tree.
Task 23: scoped re-review dispatched (model haiku, fix range 41a7892..13bea39) — instructed to reason about the guard's behaviour from the code rather than infer safety from a passing test, to confirm the three original checks survive unweakened and legitimate paths are not over-rejected, and to confirm byte-for-byte that the four new cases are purely additive and that routes.test.tsx and aria-invalid were left alone.
Task 23: re-review — FINDING ADDRESSED, no new breakage. The guard is now `if (!raw || !raw.startsWith("/") || raw.startsWith("//") || raw.includes("\\"))` at formErrors.ts:13 — purely additive, with all three original checks present and unweakened, verified by behaviour rather than by the presence of a passing test. //evil.test, https://evil.test, empty, and whitespace-prefixed values all still return "/", and legitimate paths (/ and /a/b?c=d) pass through unchanged, with the query-string case re-added as an explicit positive control against over-rejection. No new URL(...)/.origin and no window.location in the helper. Test file shows ZERO deletions (four added it() blocks, five pre-existing assertions byte-for-byte unchanged), and the diff touches exactly two files — neither routes.test.tsx nor anything aria-invalid. Trailer confirmed on 13bea39.
Task 23: fix round 1/5 (1 addressed, 0 open; commits 41a7892..13bea39)
Task 23: complete (commits 1b4b7fe..13bea39, review clean after 1 fix round)
Task 24: dispatched (base 13bea39, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-24-brief.md). Provider settings page.
Task 24: implementer DONE, commit b03c50c "feat(frontend): add the provider settings page". Frontend gate clean (328/328, lint, format:check, build with dist/.gitkeep restored before commit) plus a backend go build sanity check. Mutation ratios: no-token-key-on-unchanged-edit 1/1 (after the implementer caught its OWN first mutation as non-discriminating — the service layer already strips a falsy token independently, so the initial mutation could not have failed; it re-ran a real one, which is exactly the self-scrutiny the measurement rules ask for), empty-state-CTA-reachable 2/2.
Task 24: three declared deviations, all routed to the reviewer rather than accepted on report. (1) a native <select> for provider kind instead of the Radix Select primitive, justified only by "no test in this repo yet exercises Radix Select in jsdom" — I flagged that to the reviewer as possibly a test-convenience choice trading away UI consistency, and asked it to check whether components/ui/select.tsx exists and whether any shipped page already uses it. (2) `validate: true` HARDCODED in the create payload because the Zod schema has no such field and the brief gives no UI spec for it — I ordered the reviewer to verify against the actual backend handlers and auth/providers.go what `validate` does and what the absent-default is, since if it triggers live credential verification against the provider API then hardcoding true is a real behavioural decision, not a filler value. (3) no action slot added to EmptyState, relying on the page's persistent header "Add provider" button for the empty-state CTA.
Task 24: task reviewer dispatched (model sonnet, range 13bea39..b03c50c), additionally charged with confirming that no provider token is ever rendered, logged, or placed in a URL, and that an edit leaving the token field blank does not clear the stored token.
Task 24: review — spec ✅ (all 12 brief scenarios implemented with passing tests, verified individually), quality Approved, 0 Critical, 0 Important, 3 Minor. Token safety confirmed at the level that matters: the list renders only `•••• {tokenLast4}`, a test asserts no ghp_ pattern appears and that the tail occurs exactly once, autoComplete="new-password" and type="password" are set on both forms, and no token reaches a log, URL or query string. The blank-token-keeps-stored-token rule is enforced at TWO independent layers (the form omits the key, and the service drops a falsy token) — judged genuine defence in depth rather than redundancy. No localStorage/sessionStorage, no `status === 401` anywhere in the diff, flat model types with no Resource<> leakage, applyServerError reused rather than reimplemented, and the Task 22 placeholder fully replaced with no leftover comment or parallel shell. Error codes cross-checked against settings_providers.go:172-174. Standalone equivalence trivially satisfied — both test files are new, no pre-existing test touched. Trailer confirmed on b03c50c.
Task 24: deviation (2) RESOLVED IN THE IMPLEMENTER'S FAVOUR by backend verification, which is why I ordered it. settings_providers.go:96-117 shows `Validate *bool` is a real field that defaults to TRUE when absent on create and drives actual server-side credential verification via internal/auth/verify.go. So hardcoding `validate: true` is behaviourally identical to the server's own default and is the value the brief's acceptance test expects in the POST body — not dead weight and not a hidden behavioural change. No finding.
Task 24: deviation (3) RESOLVED. FR-5.9 asks for "a call to action, not an empty table", not a button inside the EmptyState box. The header "Add provider" button renders unconditionally whenever the panel is closed, which is always true when the empty state shows, and one test asserts the empty-state text and the button in the same render. Letter and spirit satisfied; duplicating the CTA inside EmptyState is a preference, not a spec gap.
Task 24: minor (deferred): deviation (1), the native <select> for provider kind, is a real consistency regression — components/ui/select.tsx EXISTS and ProviderPicker.tsx already ships a Radix Select, so "untested in jsdom" was a test-authoring gap, not an untested-territory constraint, and the implementer's own report conceded the shadcn-first convention. Not a correctness defect (the native element is labelled, keyboard-operable and registers with react-hook-form), so it does not block. FOR THE FINAL REVIEW TO TRIAGE: swap to Radix Select with the needed jsdom scaffolding (e.g. a pointer-capture polyfill), or accept the native element deliberately — but do not let it become a standing pattern by default.
Task 24: minor (deferred): a reviewer could prefer the empty-state CTA duplicated inside EmptyState for scanability. Preference only.
Task 24: complete (commits 13bea39..b03c50c, review clean, 0 fix rounds)
Task 25: dispatched (base b03c50c, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-25-brief.md). Account settings page — the LAST of the four screens. This is the screen where the narrowed 401 contract is load-bearing in practice: a wrong current password on change-password or delete-account returns INVALID_CREDENTIALS from a FULLY LOGGED-IN user and must render inline, never log them out.
Task 25: implementer DONE, commit 1b780c0 "feat(frontend): add the account settings page". Frontend gate clean (337/337, lint, format:check, build with dist/.gitkeep restored) plus a backend go build / targeted go test -race cross-boundary check. Mutation ratios 3/3 — INVALID_CREDENTIALS field-mapping mutant, queryClient.clear() removal mutant, and the delete-button isValid gate mutant.
Task 25: SELF-REPORTED FALSE NEGATIVE, and it is the most valuable line in this report. One mutation test initially did NOT discriminate because a gcTime setting on the local test QueryClient masked the removal of queryClient.clear(); the implementer noticed and corrected it. This is the second time in this plan an implementer has caught its own non-discriminating mutation (Task 24 was the first). I routed it to the reviewer for an independent sanity check that the CORRECTED test genuinely fails when queryClient.clear() is removed, because a cache-clear assertion that silently passes either way is exactly the kind of test that would let this screen's worst failure mode through unnoticed.
Task 25: three declared deviations, all routed to the reviewer. (1) the query-cache clear lives in the PAGE rather than the hook — I asked the reviewer to judge completeness in both directions: cleared on every session-ending path (logout, account deletion) and on NO path that is not (a failed password change, a mistyped current password), since a cache clear on a wrong-password path is precisely this screen's risk. (2) AccountDetails prop types adjusted for exactOptionalPropertyTypes — to be confirmed a genuine type-correctness fix rather than a widening that hides a missing value. (3) a local test-only provider-stack helper instead of extending the shared render.tsx.
Task 25: task reviewer dispatched (model sonnet, range b03c50c..1b780c0), with the mistyped-current-password case as the headline item: verify FROM THE CODE, not from a passing test, that a logged-in user who mistypes their current password sees an inline field error, stays logged in with the global handler not firing, and does not have their query cache cleared — and that a test exists which would actually fail if that regressed.
Task 25: review — spec ✅ (all 8 brief scenarios present and correct), quality Approved, 0 Critical, 0 Important, 1 Minor. Trailer confirmed on 1b780c0. Placeholder confirmed replaced in place against the b03c50c version — the Task 22 placeholder obligation is now FULLY DISCHARGED across all four screens.
Task 25: HEADLINE ITEM VERIFIED STRUCTURALLY, NOT JUST BY TEST — this is the answer to the risk that has been tracked since Task 21. A mistyped current password cannot log the user out BY CONSTRUCTION: ChangePasswordForm's catch calls only applyServerError and the component does not even import useQueryClient, so it has no way to clear the cache; useChangePassword invalidates only authKeys.me onSuccess, so nothing fires on failure; and DeleteAccountForm's queryClient.clear() sits AFTER the await inside the try block, so a thrown INVALID_CREDENTIALS jumps to catch and structurally cannot reach it. client.ts:57 guards with `status === 401 && error.code !== "INVALID_CREDENTIALS"`. Grep confirms zero blanket 401 handling and zero localStorage/sessionStorage in the diff. The dedicated test asserts the field error, aria-invalid="true", the username still on screen, and navigate not called.
Task 25: deviation (1) CONFIRMED CORRECT AND COMPLETE IN BOTH DIRECTIONS — the cache clear fires on exactly the two session-ending paths (delete success, and useLogout's pre-existing clear on the account-menu logout) and on no failure path.
Task 25: deviation (2) CONFIRMED A GENUINE TYPE FIX, not a widening that hides a missing value. tsconfig.app.json:24 has exactOptionalPropertyTypes: true live, under which `username?: string` would FORBID explicitly passing undefined — which `user?.username` can produce — so `string | undefined` is the required form.
Task 25: the self-reported false negative CHECKED AND CREDIBLE — renderPageWithClient now uses gcTime: Infinity and the queryClient.clear() removal mutant is caught against the corrected test. The shared queryWrapper's gcTime: 0 default is unrelated and untouched.
Task 25: minor (deferred): the local renderPageWithClient helper (~20 lines) re-declares the provider stack instead of extending render.tsx's existing renderWithProviders/queryWrapper pattern, which already demonstrates the precedent of exposing the client. Backward-compatible cleanup, not a defect.
Task 25: complete (commits b03c50c..1b780c0, review clean, 0 fix rounds)

=== FRONTEND SCREENS COMPLETE (Tasks 21-25). Remaining: 26 (two cross-cutting security assertions), 27 (hosted integration test + the flake obligation), 28 (docs/ops/full build gate), 29 (code review before PR). ===

## HANDOFF at Task 25 (2026-09-11, session 5)
Session 5 ran Tasks 22, 23, 24, 25 plus one fix round (Task 23's open-redirect fix). Controller stopping at the context-handoff threshold on a clean boundary: all four screens are built and reviewed, so the next unit — Tasks 26-27, the cross-cutting security assertions and the hosted integration test — is genuinely new work.
Resume by: `/clear`, then `/execute-task task-005` from the worktree. Base for Task 26 is `1b780c0`. Briefs through Task 25 are extracted in this workspace; Task 26's must be extracted with scripts/task-brief.
CARRY FORWARD, in priority order:
1. THE FLAKE. The intermittent internal/auth timing flake was sighted twice in earlier sessions by different agents under full-suite load. It did NOT appear in any of session 5's runs (four or more clean full-suite runs across Tasks 22-25). That is evidence but not proof — absence under a frontend-heavy workload says little. It must still be either reproduced and fixed or POSITIVELY ruled out before the PR. It belongs to Task 27 and the final review.
2. Deferred minors for the final review to triage: Task 24's native <select> instead of the shipped Radix Select (the only one with any design weight — decide deliberately, do not let it become a standing pattern); Task 25's renderPageWithClient duplicating render.tsx's provider stack; Task 22's ErrorBanner prop name error in its report only; Task 22's non-exhaustive ModeGate/AccountMenu mutation coverage; Task 21's mildly redundant client.test.ts 401 tests.
3. Task 28 owes FOUR documentation corrections carried from session 3 and 4, listed in execution-notes-4.md — do not lose them.
4. npm run build writes into apps/backend/internal/ui/dist and can dirty the tree (dist/.gitkeep); restore before committing.

## SESSION 6 (2026-09-11)
Task 26: dispatched (base d8e5922, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-26-brief.md). The two cross-cutting security assertions: no secret reaches a log, no response body carries a provider token. Note base is d8e5922 (the session-5 docs commit), not the 1b780c0 the session-5 handoff named — the handoff was written before that docs commit landed.
Task 26: RULING on the brief's `"token"` substring assertion — narrow it to `"token":` (attribute form with colon). The brief's literal `strings.Contains(body, "\"token\"")` would fire on the brief's OWN vacuity guard value `"tokenLast4":"9f2c"`, making the two assertions in the same loop mutually unsatisfiable. This is a finding against plan text and mine to decide; the spec's intent (FR-5.4, no plaintext token attribute on the wire) is served exactly by the colon form, and the mask guard is preserved untouched. Cost if wrong: a response carrying a bare `"token"` key with no colon after it would slip through — no such JSON shape exists.
Task 26: RULING on unreachable routes in the no-token walk — issue every route in the brief's list and capture ERROR bodies too rather than dropping routes that cannot be driven to 2xx with the available fakes. The assertion is over all captured bodies, and an error body leaking a token is exactly as bad; silently shortening the walk would quietly reduce the coverage the brief specifies. Cost if wrong: none material — extra non-2xx bodies only widen the assertion surface.
Task 26: the flake obligation was carried INTO this dispatch rather than waiting for Task 27 — the implementer is instructed to capture any internal/auth failure verbatim and never retry-until-green, and to state explicitly when the full suite is clean so the evidence keeps accumulating.
Task 28 OWED DOC CORRECTIONS (carried from sessions 3-4, restated here so they survive compaction): (1) the Task 12 `scopeFrom` plan-text error; (2) the Task 14 forward reference to `Service.ListProviders`; (3) api-contracts.md:115's logout wording; (4) Task 20's unsatisfiable Step 4 check (session-4 ruling 3). Also reconcile the plan's "logout registered OUTSIDE authenticate" wording with the implemented `publicRoute` exemption (session-4 ruling 2).
Briefs for Tasks 27, 28 and 29 pre-extracted to this workspace while Task 26 ran.
Task 26: implementer DONE, commit 2283361 "test: assert no secret reaches a log and no response carries a token". Full backend gate clean (go test -race, -tags integration, go vet, golangci-lint, CGO_ENABLED=0 build) with NO internal/auth flake on either full-suite run — that is now roughly the sixth consecutive clean full-suite run across sessions 5-6, still evidence and not proof. Mutation ratios 4/4: the secret-in-log mutation, its vacuity guard, the token-in-response mutation (both the raw-token and "token": assertions fired), and its vacuity guard were each confirmed to discriminate.
Task 26: TWO declared deviations, both routed to the reviewer rather than accepted on report. (a) app.New has no writer seam for the brief's app.NewLogger(buf, cfg), so the implementer redirected os.Stderr before calling app.New — it argues this observes the genuine production-wired logger instead of a hand-built stand-in. (b) app.New's hosted resolver has no seam for internal/provider/fake, so the brief's "against a fake provider" became a local httptest.Server 404 stub.
Task 26: deviation (b) is the LOAD-BEARING RISK and I made it the reviewer's headline item. Provider create defaults to validate:true and does live credential verification, so a 404 stub may make POST /api/settings/providers FAIL — in which case the provider-token sentinel was never stored, never encrypted and never handled, and "the provider token did not reach the log" passes for the trivial reason that nothing processed it. The brief's own logged!="" and user_id= guards do NOT cover this: they prove the logger ran, not that the provider path ran. Reviewer ordered to establish from actual response statuses whether each of the seven scripted steps succeeded.
Task 26: task reviewer dispatched (model sonnet, range d8e5922..2283361) with four named adjudication items — the vacuity risk above, the os.Stderr seam judged in BOTH directions (stronger coverage vs. capturing less than intended, plus restore/race safety), the "token": narrowing verified no weaker than the brief's intent with the tokenLast4 guard intact, and route-walk completeness against the brief's list.
Task 26: review — spec ✅ but quality NEEDS FIXES. 1 Critical, 1 Important, 1 Minor. The headline adjudication item I ordered PAID OFF and was reproduced directly by the reviewer rather than argued.
Task 26: THE CRITICAL FINDING. POST /api/reviews in secrets_test.go returns 404 REPOSITORY_UNAVAILABLE, not 202. review.Service.Create (internal/review/service.go:129) calls p.GetRepository SYNCHRONOUSLY before the session is ever persisted; against the implementer's 404 httptest stub this fails and createReview (internal/api/reviews.go:93-97) returns the error WITHOUT ever reaching StartBuild. So the async build and the git-credential-injection path (GIT_CONFIG_KEY_0/VALUE_0 via gitx.Runner) — precisely the mechanism design §10 and the global constraints single out as where a token-into-argv/URL regression would most plausibly happen — is NEVER EXERCISED by the test that exists to protect it. The `_ = do(http.MethodPost, "/api/reviews", ...)` call has no status assertion, so nothing surfaced it.
Task 26: the finding is NARROWER than full vacuity, and the distinction matters. Register 201, login 200 and provider create 201 all genuinely succeed, so the password, session-token, master-key and provider-token-at-rest (encryption) sentinels ARE meaningfully covered. GetRepository also makes one real synchronous HTTP call carrying the raw token in a PRIVATE-TOKEN header, so the provider-token sentinel is exercised through the provider-fetch path. What is missing is the build/clone/log path specifically.
Task 26: the implementer achieved provider create 201 by adding `"validate":false` to the request body — not in the brief, and undeclared in its report. Harmless and arguably necessary (it sidesteps the live-validation trap), but it is the second undeclared thing in this report.
Task 26: RULING — FIX NOW in Task 26, via the reviewer's option (b). Use internal/provider/fake's registered "fake" kind to drive review create to success so StartBuild actually runs, while KEEPING a separate gitlab-kind provider so POST /api/settings/providers still exercises the token-encryption path. Reasoning: this is not a finding against plan text to be weighed — the brief literally says "against a fake provider" and the 404 httptest stub defeated the brief's own intent, so fixing it restores the brief rather than overriding it. Decisive factor: no_token_test.go in this same commit ALREADY drives the fake kind successfully, so the capability is proven present in the repo and the fix is not speculative. Cost if wrong: a longer, slower test that exercises a real build path; acceptable for the one test whose entire purpose is asserting a whole-system security property.
Task 26: the Important finding is PROMOTED into the same fix round, not deferred, because the Critical fix makes it live. The hand-assembled api.Deps omits BuildContext among other fields; while StartBuild was never reached that was latent, but once review create succeeds StartBuild WILL run and would receive a zero-value context. Fixing the Critical without this would convert a latent nil-context into an executed one.
Task 26: fix round 1/5 — fresh task-implementer dispatched (SendMessage re-confirmed unavailable this session via ToolSearch), carrying the brief path, the report path, both findings, the prescribed option-(b) shape, a requirement to ASSERT the status of every scripted step so a silent non-2xx can never hide again, and an instruction to declare the validate:false addition in the report.
Task 26: fix round 1 implementer DONE, commit 5701497 "fix(task-005): make TestNoSecretReachesTheLog actually exercise review create". Full backend gate clean (go test -race, -tags integration, go vet, golangci-lint 0 issues, CGO_ENABLED=0 build), no internal/auth flake on either full-suite run. Mutation ratio 2/2 re-run against the CHANGED path — secret-leak mutant caught at secrets_test.go:360 ("a secret reached the log") and the vacuity-guard mutant at secrets_test.go:365 ("nothing was logged").
Task 26: fix shape as delivered — review create is now backed by a STATIC "fake"-provider registry mirroring isolation_test.go's newHostedFixture, while the DB-backed gitlab-kind provider still exercises token encryption via POST /api/settings/providers. Every scripted step's status is asserted and the review is confirmed reaching READY. BuildContext set to context.Background() with a comment naming every api.Deps field left at zero and why. "validate":false confirmed still present and now documented inline.
Task 26: one NEW declared deviation in the fix — an explicit READY-status assertion beyond the brief's literal step list, to make "the build genuinely ran" provable rather than inferred.
Task 26: scoped re-review dispatched (model sonnet, fix range 2283361..5701497) with one judgment call made explicit and load-bearing: does the static-registry substitution genuinely CLOSE finding 1 or merely RELOCATE it? Specifically whether StartBuild's path actually invokes gitx.Runner with GIT_CONFIG_* credential injection or whether the fake provider short-circuits the clone so the credential path is STILL never exercised — in which case the finding is NOT ADDRESSED no matter that the review reaches READY — and whether routing review create through a static registry LOST any coverage that existed before, since the encrypted DB-stored token may now be touched only by the settings endpoint. Reviewer also told to check the READY assertion for timing/flakiness and to confirm the three first-review adjudications were left undisturbed.
Task 26: re-review — Finding 2 ADDRESSED (BuildContext set at secrets_test.go:247 with a comment naming every remaining zero-valued field), Finding 3 ADDRESSED, NO new breakage. Finding 1 NOT ADDRESSED, and the judgment call I ordered is exactly what caught it: StartBuild now genuinely runs and the review reaches READY, but the static registry holds fake.New("fake", provider.KindGitLab) and fake.Provider.AuthorizeGit (internal/provider/fake/fake.go:177) is a hardcoded no-op returning nil. gitx.CredentialEnv is invoked ONLY from the github and gitlab clients' own AuthorizeGit, so GIT_CONFIG_* is still never populated. My prescribed option-(b) fix RELOCATED the problem rather than closing it.
Task 26: also established by the re-review — no coverage was LOST by the split. Pre-fix, review create used the DB-backed provider but never reached StartBuild, so the credential path was unexercised before and after. Token-encryption-at-rest IS covered by the settings-provider step; git-credential-injection is covered by neither, and never was.
Task 26: RULING on Finding 1 — PARK, with reasoning, rather than spend rounds 3-5 on it. Three grounds. (1) PLAN-TEXT CONFLICT: the brief's step 2 literally specifies "review create (against a fake provider)", and a fake provider structurally CANNOT inject credentials, so closing the finding requires contradicting the brief — which makes this mine to rule on now rather than premature adjudication. (2) STRUCTURAL EVIDENCE GATHERED FROM SOURCE, not assumed: gitx never logs Args or Env at all. internal/gitx/exec.go:159-182 logs only git.category, repository, session, exit_code, duration_ms, outcome, and stderr — and stderr goes through Redact(). So a credential placed in Spec.Env cannot reach a log via argv or env by construction; the worry Finding 1 names is answered by the code even though it is not answered by the test. (3) Task 27 cannot close it either — its brief also specifies a local-git-backed FAKE provider, so the same no-op AuthorizeGit applies. Cost if wrong: if some future code path logs Spec.Args or Spec.Env, no test in this plan would catch it. Mitigation: routed to the final review (Task 29) as a named residual.
Task 26: NEW FINDING discovered while grounding the ruling above — and it is a PRODUCT gap, not a test gap. Verified from source: there is ONE shared gitx.Runner (review.Deps.Runner), built once at internal/app/app.go:241 with Secrets: cfg.Secrets(). config/config.go:104-118 shows Secrets() collects ONLY env-configured c.Providers tokens plus the master key. In HOSTED mode provider tokens live per-user in the DATABASE and are never in cfg.Providers — so no hosted user's token is in the redaction list. Two surfaces are affected: gitx stderr redaction (exec.go:176, Redact(stderrLog, r.opts.Secrets)) and the redactingHandler itself (app.go:199, built from cfg.Secrets()). A hosted per-user token appearing in git stderr or in any log attr would be logged IN CLEAR. This is precisely the accident Task 26's brief says the redaction list exists to catch, and Task 26's own test passes only because the sentinel token never reaches those paths in the exercised flow. ESCALATED TO THE USER as a scope decision — fixing it needs dynamic per-user secret registration, which is real design work beyond this plan.
Task 26: complete (commits d8e5922..5701497, 1 fix round, 1 parked: Finding 1 credential-injection path unexercised — parked with the structural ruling above).
USER DECISION (2026-09-11, session 6): the hosted per-user token redaction gap is to be FIXED INSIDE task-005, not deferred to a follow-up. This adds a task beyond the plan's 29 and lengthens the branch; the user chose this over deferring and over an exploitability investigation first. Recorded here because it is a scope change the plan does not contain — the plan's Global Constraints assert "No password, token, cookie value, or master key is ever logged" as a binding requirement, so this fix is closing a genuine violation of a stated constraint rather than adding a feature.
NEW TASK 30 (queued behind Task 27; NOT from plan.md): register hosted per-user provider tokens for log redaction. Do not dispatch it in parallel with Task 27 — the SDD rule forbids concurrent implementers.
Task 30 design notes gathered from source BEFORE dispatch (the lesson of Task 26's failed option-(b) ruling: do not prescribe without grounding):
  - internal/gitx/exec.go:27 Options.Secrets []string is fixed at construction; exec.go:176 redacts stderr with Redact(stderrLog, r.opts.Secrets).
  - internal/gitx/spec.go:25-34 Spec already carries per-invocation fields (Dir, Args, Env, Stdin, Category, Timeout, Repo, Session).
  - The single shared runner (review.Deps.Runner) has no idea which user a given build belongs to, so a global "all users' tokens" list would mean decrypting every provider token on every log line — expensive and itself a risk. REJECT that shape.
  - RECOMMENDED SHAPE (to be validated by the implementer, not taken on faith): add per-invocation secrets to gitx.Spec and redact with opts.Secrets + spec.Secrets. The token already enters gitx through the provider's AuthorizeGit (github/client.go:183-190, gitlab/client.go:232-239, which call gitx.CredentialEnv), so AuthorizeGit is the natural place to also declare the secret — scoped, no global registry, no decryption fan-out.
  - Second surface, harder and to be assessed separately: the redactingHandler at app.go:199 is also built from cfg.Secrets() and would still miss a hosted token reaching a general log attr.
Task 27: implementer DONE_WITH_CONCERNS, commit 14b04b1 "test(review): add the hosted-mode isolation and prune integration tests". Full gate clean — go test -race -count=1 ./... clean TWICE, -tags integration ./... clean, go vet clean, golangci-lint 0 issues, CGO_ENABLED=0 build clean. Both new tests pass. Mutation ratios 1/1 and 1/1: the namespace-collapse mutant was caught ("A and B share one mirror path") and the Store.Get scope-bypass mutant was caught ("A can see B's session").
Task 27: THE FLAKE — NOT REPRODUCED across 6 executions this task (3 full-suite runs, 1 dedicated go test -race -count=3 ./internal/auth/, 1 integration-tagged full-suite run), all clean. Combined with sessions 5-6 that is roughly a dozen consecutive clean runs including a dedicated -count=3 hunt of the exact package. This is now strong evidence, and the implementer correctly reported it as "not reproduced under N runs of these shapes" rather than claiming it ruled out. Final call belongs to the final review (Task 29).
Task 27: two declared brief conflicts, both routed to the reviewer. (1) the brief's sample says `package review_test` but the harness it ALSO instructs reusing (newHarness) is unexported and lives in `package review` — two brief instructions genuinely contradict, and the implementer chose harness reuse. (2) the brief's literal POST /api/settings/providers / auth.Service.CreateProvider path can only construct real github/gitlab clients because no local-git config.Kind exists, so the implementer wrote a minimal in-test provider.Resolver mapping each user id to its own fake.Provider/Registry, bypassing internal/auth and internal/db entirely.
Task 27: conflict (2) is the HEADLINE REVIEW ITEM and the one with real risk. The brief calls this test "the isolation acceptance criterion at full depth"; if the in-test resolver stubs out enough of the stack, the isolation being asserted becomes the TEST'S OWN construction rather than the product's. Reviewer ordered to name precisely which production components are genuinely exercised (real mirror.Cache namespacing? real session.Store scope filtering? real review.Service build path?) versus replaced, and to treat a substitution reaching further than the implementer's summary as a finding.
Task 27: task reviewer dispatched (model sonnet, range 5701497..14b04b1), also charged with verifying each load-bearing assertion FROM THE CODE — in particular that the byte-identical-diff check compares real combined-diff BYTES rather than lengths or file counts, and that the prune test would actually fail if ExcludeReviewRefspec protection were lost.
Task 27: review — spec ✅, quality APPROVED, 0 Critical, 0 Important, 0 Minor. Both declared conflicts resolved in the implementer's favour on evidence, not on report.
Task 27: conflict (2) RESOLVED — the in-test resolver does NOT hollow out the isolation claim, and the reviewer proved it structurally. provider.Resolver (internal/provider/resolver.go:20) is a PUBLIC interface whose doc comment says the hosted implementation lives in internal/auth and "satisfies this interface from the outside", and auth.ProviderResolver declares `var _ provider.Resolver = (*ProviderResolver)(nil)` (internal/auth/resolver.go:60) — so the real hosted resolver is itself just another implementer of the same interface and the test's scopedResolver occupies the IDENTICAL position in the dependency graph. Everything downstream is production: mirror.Cache, session.Store, review.Service, gitx.ExecRunner. The claim that the brief's literal path is unreachable was also verified rather than accepted — config.go:20-21 defines only KindGitHub/KindGitLab, so auth.buildClient has no local-git branch. The isolation asserted is the PRODUCT'S, not the test's own construction.
Task 27: conflict (1) RESOLVED — `package review` is correct and my dispatch constraint repeating the brief's `review_test` was wrong. Every existing test file in the package is `package review`, including integration_test.go which the brief itself says to reuse; no review_test file exists anywhere in the package. The new file's ONLY unexported dependency is testLog(); everything else it touches is exported. No black-box fidelity lost beyond what the whole package already accepts.
Task 27: load-bearing assertions all verified FROM THE CODE — the byte-identical check is a real `string(rawA) != string(rawB)` over os.ReadFile of CombinedDiffPath (not lengths or file counts), and the prune test's branch check runs through the real gitx.Runner against the real post-prune mirror. Reviewer confirmed Build's resolve pipeline already calls Ensure once, so the test's explicit second Ensure genuinely takes the `fetch --prune ... ExcludeReviewRefspec` branch (mirror/cache.go:134-152) rather than the clone branch — and that if ExcludeReviewRefspec were dropped, MirrorRefspec "+refs/*:refs/*" (gitx/refs.go:10) would prune the local-only review/<id> ref and the assertion would fail.
Task 27: complete (commits 5701497..14b04b1, review clean, 0 fix rounds)
Task 30: dispatched (base 14b04b1, model OPUS — this is the first source-code task of the session and a security fix requiring design judgment, so it gets the capability tier the SDD Model Selection rules reserve for architecture work; every other task this session was sonnet). Register hosted per-user provider tokens for log redaction.
Task 30: dispatch structured to guard against the failure mode that cost Task 26 a round — my recommended shape (per-invocation secrets on gitx.Spec, declared at AuthorizeGit where the token already enters gitx) is given WITH the reasoning and an explicit instruction to validate it against the code rather than take it on faith, plus permission to choose a better shape if the code supports one. The global all-users-token list shape is explicitly REJECTED in the dispatch with its reason.
Task 30: dispatch also requires a FAILING TEST FIRST demonstrating the leak, and explicitly authorises NEEDS_CONTEXT if the defect does not reproduce — i.e. if some path I missed already registers per-user tokens. I would rather learn the defect is not real than have a fix built for a non-problem.
Task 30: the second surface (redactingHandler at app.go:199, a hosted token reaching a general log ATTRIBUTE rather than git stderr) must be either fixed or documented with file:line evidence that it is unreachable. Silence on it is explicitly disallowed.
Task 30: standalone equivalence flagged as a REAL risk rather than a formality for this task specifically, because unlike every other task this session it changes shared source (gitx) rather than adding a test file. Additive/backward-compatible change preferred so existing callers are untouched.
Task 30: implementer DONE, commit 5fcb4cf "fix(task-30): redact hosted per-user provider tokens from git stderr logs". Full gate clean (go test -race, -tags integration, go vet, golangci-lint 0 issues, CGO_ENABLED=0 build), no internal/auth flake in either race run.
Task 30: THE DEFECT WAS REAL AND WAS DEMONSTRATED BEFORE BEING FIXED — this is the part that matters. Pre-fix failure, quoted: authorize_git_secret_test.go:67: "hosted per-user token written to the log in clear: ... stderr=\"fatal: 'ghp-hostedusertoken0123456789' does not appear to be a git repository". So the gap I escalated to the user was not theoretical; a hosted token reached the log in clear.
Task 30: fix shape as delivered — per-invocation secrets on gitx.Spec, redacted as the union r.secretsFor(s) of opts.Secrets and the spec's own, with both github and gitlab AuthorizeGit declaring the token. This is the shape I recommended and the implementer validated it against the code rather than taking it on faith. Files: internal/gitx/exec.go, internal/gitx/spec.go, internal/provider/github/client.go, internal/provider/gitlab/client.go plus four new test files.
Task 30: mutation ratios 3/3 in BOTH directions, which is stronger than the usual single-direction check. Reverting the union redaction (r.secretsFor(s) -> r.opts.Secrets) fails 3/3 new-behaviour tests while the preserved-behaviour test correctly STILL PASSES — that second half is the evidence that standalone behaviour is genuinely unchanged, not merely asserted. Dropping the spec.Secrets append in both AuthorizeGits also fails 3/3 ("AuthorizeGit did not declare the token as a spec secret: []").
Task 30: step 3 (the redactingHandler second surface) was ASSESSED, not skipped — concluded unreachable on evidence: config.Secret implements LogValue/String/MarshalJSON all returning [redacted] (internal/config/secret.go:19,22,25), every decrypted hosted token is wrapped at decryption (internal/auth/crypt.go:87), and the only non-test Reveal() calls are two HTTP headers, two CredentialEnv calls, the redaction list itself and the master-key base64 decode. Git stderr is confined to internal/gitx, and gitx.ExitError.Error() does not embed stderr. I have ORDERED THE REVIEWER TO INDEPENDENTLY RE-GREP Reveal() rather than accept the enumeration, because signing off a security surface on an incomplete enumeration is exactly how this kind of gap survives.
Task 30: one declared concern — the end-to-end test puts the token in ARGV to force git to echo it on stderr, commented as a simulation. Routed to the reviewer as a named item, since the Global Constraint says credentials reach git ONLY via GIT_CONFIG_* and never argv; the reviewer must confirm production flow is unchanged and state what residual risk the test does and does not retire.
Task 30: task reviewer dispatched (model OPUS, range 14b04b1..5fcb4cf) with five adjudication items — standalone-equivalence blast radius (gitx is shared by every git call and this is the branch's ONLY source change), whether AuthorizeGit is truly the only seam a per-user token enters a Spec, whether union redaction covers ALL FOUR stderr log sites in exec.go (timeout / non-zero exit / failed-to-start / ok) rather than one branch, whether Spec.Secrets itself creates a NEW leak surface via %v/%+v/slog.Any/ExitError (the cure must not be the disease), and independent verification of the Reveal() enumeration.
Task 30: review — spec ✅, quality APPROVED, 0 Critical, 0 Important, 5 Minor. All five adjudication items came back clean.
Task 30: item 1 (blast radius) CLEAN AND CAREFULLY CHECKED. secretsFor returns r.opts.Secrets BY IDENTITY when the spec declares none (exec.go:113-115), so standalone is bit-for-bit the prior behaviour; the non-empty branch allocates a fresh slice with exact capacity (exec.go:116-119) so it cannot append into opts.Secrets' backing array — the aliasing hazard was considered and closed. The reviewer checked ALL 36 `gitx.Spec{` literals in the tree and confirmed every one is KEYED, so the added field breaks no literal.
Task 30: item 2 CLEAN — CredentialEnv has exactly two non-test call sites, both inside AuthorizeGit, so that is the complete seam; clone URLs come from provider API payloads and there is no url.UserPassword anywhere. And exec.go has exactly ONE stderr log site (:190), not four — the timeout, failed-to-start and ok branches log only attrs. My worry about an uncovered second branch was unfounded.
Task 30: item 3 CLEAN — Spec has no String/MarshalJSON/LogValue, is never logged or serialized, is passed by value and never stored, and gitx.ExitError holds {Category, Result} with no Spec, so Secrets cannot reach it. The cure is not the disease.
Task 30: item 5 CLEAN AND INDEPENDENTLY RE-GREPPED, which is why I ordered it. The reviewer's own Reveal() enumeration returned exactly ten non-test hits matching the report with NO omissions. The decision to leave redactingHandler static is correct and correctly justified.
Task 30: ITEM 4 — THE CORRECTION, and it revises the branch narrative. The reviewer establishes that the production credential path is untouched AND that there is NO currently reachable production path by which a RAW hosted token lands on git stderr: the token exists in git's world only as base64 inside an extraheader Basic blob. The implementer's failing test manufactured the condition by writing the token into a test-local Spec.Args. So this fix is DEFENCE IN DEPTH against a hypothetical channel, not the closure of a demonstrated live exfiltration. The report's "logged in clear" framing — which I repeated to the user — overstates present reachability. The honest statement is "would be logged in clear if it ever surfaced". CORRECTED TO THE USER.
Task 30: RULING — take the bare-blob Minor into a fix round rather than deferring it, even though Minors do not normally enter the loop. Reasoning: item 4 establishes the BLOB, not the raw token, is the form that actually exists in git's world, so the blob is the realistic leak vector — and AuthorizeGit currently declares only the raw token. redact.go:8's headerRe covers the blob ONLY when the literal authorization:/private-token: prefix accompanies it in the stderr text, so a bare blob is redacted by neither list. The reviewer's words: "reasonable but not airtight". This is one more append and it closes the only channel with any production reality; deferring the most load-bearing half of a security fix the user explicitly chose to make now would be the wrong call. Cost if wrong: a few more bytes in a redaction list.
Task 30: the other four Minors DEFERRED to the final review — ExitError.Result.Stderr raw exported field (pre-existing, could carry unredacted stderr via slog.Any under the JSON handler; consider a LogValue() follow-up), the gitlab test filename implying an end-to-end stderr test that does not exist (GitHub-only, defensible since the mechanism is shared), spec_secrets_test.go:45 discarding the Run error inconsistently with its siblings, and authorize_git_spec_secret_test.go:36-40 asserting the raw token is absent from spec.Env which passes TRIVIALLY because the token is base64'd and so guards nothing.
Task 30: fix round 1 implementer DONE, commit 55e5af1 "fix(task-30): declare base64 Basic blob as a redaction secret, tighten tests". Full gate clean (go test -race, -tags integration, go vet, golangci-lint 0 issues, CGO_ENABLED=0 build), no internal/auth flake in either run. Finding A mutation ratio 2/2 with quoted assertion failures from a /tmp scratch copy; findings B and C both fixed as specified.
Task 30: THE FIX GREW BEYOND ITS SCOPE and that is now the re-review's headline item. It was scoped to "one more append" but the diff touches internal/gitx/credentials.go and introduces a NEW EXPORTED FUNCTION gitx.BasicAuthBlob in shared source. That is not automatically wrong — sharing one derivation is exactly how you stop the declaration and CredentialEnv drifting apart — but it changes the function that builds the production credential env for EVERY authenticated git call in the product, so it gets scrutiny rather than a nod.
Task 30: scoped re-review dispatched (model sonnet, fix range 5fcb4cf..55e5af1) with four questions on the helper — does its blob byte-for-byte match what CredentialEnv puts in GIT_CONFIG_VALUE_0 and do BOTH sites go through ONE derivation (a re-derivation would silently un-redact on any future drift, which is precisely what I instructed against); is CredentialEnv's own output unchanged and does credentials_test.go still pass unmodified; does EXPORTING BasicAuthBlob create a misuse or leak hazard or could it have been unexported; and did the diff add any module import to gitx.
Task 30: also ordered a plain answer on the implementer's declared scope decision to skip a gitlab-side end-to-end bare-blob test — specifically whether the DECLARATION in gitlab/client.go is itself covered by any test, or whether only the shared derivation is. The precise question I asked: if the gitlab declaration could be deleted with every test still passing, say so plainly. That is the difference between shared-code reuse and an untested second call site.
Task 30: re-review — ALL THREE FINDINGS ADDRESSED, no new Critical/Important breakage. Finding A at github/client.go:196 and gitlab/client.go:243; Finding B now asserts the real invariant (blob PRESENT in spec.Env as a t.Fatalf precondition, then declared in spec.Secrets) so it cannot degenerate into a toothless check again; Finding C consistent with its siblings.
Task 30: the headline item came back CLEAN on all four questions, and the scope growth was justified. BasicAuthBlob is a SINGLE derivation — credentials.go:9-16 defines it and CredentialEnv now CALLS it, with the old inline base64 local REMOVED rather than duplicated, so the two sites cannot drift. CredentialEnv's output is a pure extraction with identical scheme/host validation and identical string construction, and credentials_test.go has ZERO diff across the fix range (verified by the reviewer running git diff on that path). Exporting is NECESSARY because both provider packages sit outside gitx and the alternative was duplicating the derivation, which would have defeated the fix's own premise. No module import added to gitx. The argv guard TestAuthorizeGitUsesEnvOnly is present verbatim and untouched.
Task 30: THE GITLAB QUESTION PAID OFF — the plain answer I demanded was the useful one. grep shows BasicAuthBlob has ZERO references in any _test.go under provider/gitlab, and gitlab/client_test.go:177-180 asserts only len(spec.Env) and GIT_CONFIG_KEY_0, never spec.Secrets. So the gitlab declaration could be DELETED and every existing test would still pass. The implementer's reasoning covered that BasicAuthBlob is correct, but not that gitlab's AuthorizeGit actually declares it — a real, currently-unguarded regression path.
Task 30: RULING — take it into fix round 2 rather than accept the reviewer's "deferred minor". Reasoning: an untested call site in a SECURITY declaration is exactly the thing that rots, and the whole reason Task 30 exists is that a redaction list silently failed to cover a case nobody had asserted. Repeating that shape inside the fix for it would be self-defeating. The reviewer's deferral is defensible as process (it is a coverage gap, not a defect this diff introduced) but the cost asymmetry is stark: ~10 lines of test now, versus a future edit silently dropping redaction for every GitLab user with a green suite. Cost if wrong: ten lines of duplicated test.
Task 30: fix round 2/5 — fresh task-implementer dispatched (SendMessage unavailable in this harness), scoped to ONE thing only, with an explicit instruction not to touch anything else.
Task 30: fix round 2 implementer DONE, commit 23817e0 "test(task-030): guard GitLab AuthorizeGit's spec-secret declaration". Gate clean (go test -race ./... with internal/auth clean, go vet, golangci-lint 0 issues, CGO_ENABLED=0 build). Mutation ratio 2/2 with BOTH halves checked independently — deleting the blob append and deleting the raw-token append each produce a quoted failure. Exactly one new file, package gitlab, no pre-existing file touched.
Task 30: fix round 2 scoped re-review dispatched (model HAIKU — one new test file, 2.7KB diff, the cheapest tier is right for a diff this small; the SDD rule that every round ends with a re-review is not waived just because the fix is small).
Task 28: dispatched (base 23817e0, model sonnet, agent task-implementer, brief .superpowers/sdd/plan/task-28-brief.md). Documentation, ops, and the FULL CI BUILD GATE. Last implementation task on the branch.
Task 28: the FOUR OWED DOCUMENTATION CORRECTIONS were carried into the dispatch explicitly rather than left in the ledger to be found — the Task 12 scopeFrom plan-text error, the Task 14 forward reference to Service.ListProviders, api-contracts.md:115's logout wording, and Task 20's unsatisfiable Step 4 check, plus reconciling the plan's "logout registered OUTSIDE authenticate" wording with the implemented publicRoute exemption. Implementer told to report what each said before and after, and to name any it cannot locate rather than silently dropping or inventing one.
Task 28: VERIFICATION-OVER-MEMORY enforced for the env-var table — the implementer must check all seven names, defaults and behaviours against internal/config/config.go and its callers, and was told that where a documented default disagrees with the code, THE CODE IS THE TRUTH. Same for the two security statements, especially the X-Forwarded-For throttle-key-forging claim which must be checked against api/authctx.go's clientIP. A doc that confidently states a wrong default is worse than no doc.
Task 28: make docker-build may not be runnable here. Implementer explicitly required to report "could not run, here is the error" rather than claim a pass — a silent skip on the CI gate is the one thing that would make this task's completion claim worthless.
Task 28: dispatched CONCURRENTLY with the Task 30 fix-round-2 re-review. This is compliant with the SDD no-parallel rule, which forbids multiple IMPLEMENTATION subagents; only one implementer is live. File domains are disjoint (docs/ops vs one Go test file) and both agents were told about the other to prevent either misreading a dirty tree.
Task 30: fix round 2 re-review — FINDING ADDRESSED, no new breakage, no deferred minors. The new test asserts BOTH credential forms with SEPARATE assertions (blob at authorize_git_spec_secret_test.go:59, raw token at :62), so each mutation hits its own line independently rather than one assertion masking the other — that separation is what makes the 2/2 ratio meaningful. The anti-degeneration precondition is present (:37-45, t.Fatalf if the blob is absent from spec.Env). Exactly one new file added; client_test.go, credentials.go, exec.go and all GitHub files confirmed untouched. Trailer exact.
Task 30: fix round 2/5 (1 addressed, 0 open; commits 55e5af1..23817e0)
Task 30: complete (commits 14b04b1..23817e0, review clean after 2 fix rounds, 0 parked)
=== TASK 30 CLOSED. The hosted per-user token redaction gap is fixed at both credential forms, on both providers, with every declaration guarded by a mutation-checked test. ===
Task 28: implementer DONE, commit 90a7490 "docs: document standalone and hosted modes". FULL CI GATE CLEAN FROM REPO ROOT — make lint, make test, make test-integration, make build AND make docker-build all pass (docker-build genuinely ran; it was not skipped). No internal/auth flake in two more full backend runs. dist/.gitkeep left clean after npm run build.
Task 28: deliverables — README "Modes" section, new docs/hosted-mode.md, .env.example and docker-compose.yml hosted blocks, a CLAUDE.md Architecture Notes invariant amendment, and a context.md §10 acceptance-criteria-to-test mapping (35 criteria, each mapped to a test name).
Task 28: all five owed corrections handled — (1) Task 12 scopeFrom reference fixed Task 14 -> Task 16; (2) Task 14's ListProviders forward reference corrected to the actual implementation using Store.ListUserProviders; (3) api-contracts.md:115 logout wording corrected to state it is public via publicRoute; (4) Task 20's unsatisfiable Step 4 `go list -deps` check replaced with an explanatory note in BOTH plan.md and context.md; (5) the separate literal "logout registered OUTSIDE authenticate" instance COULD NOT BE LOCATED beyond #3 — the implementer flagged it rather than inventing a correction, which is the correct behaviour and must still be independently confirmed.
Task 28: one self-reported incidental — a stale "44 checkboxes" count in context.md corrected to the actual 35, verified by direct count.
Task 28: REVIEW NOT YET DISPATCHED. The context-handoff guard refused the dispatch at 62 controller tool calls. This is the correct outcome and the review must be the FIRST action of the next session.
=== SESSION 7 ===
Task 28: review dispatched (model sonnet, general-purpose, range 23817e0..90a7490, pre-generated package review-23817e0..90a7490.diff). Method INVERTED for a documentation task: a doc's defect is being WRONG, not ugly, so prose nits were explicitly devalued and seven source-verification checks were ordered instead — all seven env vars against config.go (code is the truth, a wrong default is Critical), the X-Forwarded-For throttle-forging claim against api/authctx.go clientIP, the other security statements, the five owed plan-text corrections being CORRECT rather than merely changed, independent confirmation that correction (5) is genuinely unlocatable (grep it yourself), a >=8-of-35 spot-check that the context.md §10 criterion-to-test mapping names tests that ACTUALLY EXIST plus the 35 count itself, .env.example/docker-compose consistency with a leak check, and the CLAUDE.md invariant amendment being true of the code as it now stands.
Task 28: the CI-gate claim was routed as EVIDENCE-PRESENCE ONLY — the reviewer must confirm each of the five commands has a command and a pass in the report, and flag any asserted without output, but must NOT re-run the suite. make docker-build is the one that matters since it may not be runnable in every environment.
Task 29 prep (done while Task 28's review ran): merge-base is 7ae00b9, HEAD 2260abf, 49 commits, 140 files, +24703/-271. Whole-branch package review-7ae00b9..2260abf.diff is 1.25 MB, which is too large for any reviewer to Read in one call, and 10k of its lines are docs/ (plan.md alone is 7124). So I generated SHARDED packages instead: final-code (773 KB, docs excluded), final-backend (615 KB, apps/backend + Makefile/tools/.env.example/docker-compose.yml) and final-frontend (155 KB). Reviewers get the shard matching their checklist plus an explicit instruction to read it in slices or re-diff per package rather than one Read.
Task 28: review — spec ✅ (all 7 brief steps present), quality NEEDS FIXES, 0 Critical, 2 Important, 2 Minor. The inverted method paid off: every factual axis came back TRUE against source — all 7 env var names/defaults vs config.go's modeVar/loadHosted/boolVar/durationVar, the X-Forwarded-For claim vs authctx.go clientIP (honours XFF only when deps.TrustedProxy, else RemoteAddr), secure(r) vs the CONVERGE_SECURE_COOKIES text, 10+ of 35 mapped test names grepped verbatim across internal/app, internal/config, internal/auth, internal/mirror, internal/api and 4 frontend its, the 35 count recounted directly (7+7+4+7+4+4+2), compose mounting converge-database at /data/db consistent with CONVERGE_DATABASE_PATH, and the CLAUDE.md invariant vs an actual go list -deps.
Task 28: corrections 1-4 all VERIFIED CORRECT, not merely changed — Task 16 really is where scopeFrom(r)'s body lands; service_test.go:565-591 really does alternate Login with Store.ListUserProviders as the corrected text says; authmw.go:20-24 publicRoute really does list /api/auth/logout; and the Task 20 note is accurate AND present in both plan.md and context.md.
Task 28: IMPORTANT 1 — the gate evidence is NARRATIVE ONLY. task-28-report.md:128-150 claims all five commands clean with zero raw output, no exit codes, no transcript for any of make lint/test/test-integration/build/docker-build. This is the one finding that makes the completion claim unverifiable, and docker-build is exactly the command I warned could be silently skipped.
Task 28: IMPORTANT 2 — correction (5) was NOT "genuinely unlocatable". The reviewer grepped where the implementer did not: the phrase "logout registered OUTSIDE authenticate" is verbatim at execution-notes-4.md:137, inside a carry-forward note addressed to Task 28 by name. The implementer searched only plan.md/design.md/context.md. Its ULTIMATE CONCLUSION still holds (the instance collapses into correction #3, api-contracts.md:115, already fixed, independently re-checked) so no doc is wrong today — but the record's claim is inaccurate as stated.
Task 28: RULING — take Important 1 into a fix round by RE-RUNNING the gate with output captured to a file, rather than accepting that Task 29 Step 3 re-runs it anyway. Reasoning: the deferral is tempting and wrong. What is defective is the RECORD of a claim already made, and "a later task will coincidentally cover it" leaves a completion claim in the ledger backed by nothing. Cost if wrong: one full gate run of wall-clock time.
Task 28: RULING — take Important 2 as a RECORD correction, not a doc change. The reviewer independently confirmed no documentation is currently wrong, so changing a doc would be fixing a non-defect; what needs correcting is the report's "could not locate" claim, which must name execution-notes-4.md:137 and state why it resolves into correction #3. Cost if wrong: none material.
Task 28: RULING — take Minor 2 (CLAUDE.md "Project Overview" still says "Persistence is the filesystem only" unqualified) into the SAME fix round, overriding the usual rule that Minors do not enter the loop. Reasoning: it is a flatly FALSE sentence in the very file this task amended, and a skimmer hits Project Overview before Architecture Notes — stating a wrong fact confidently is the exact defect class this task exists to eliminate. One sentence, and a fix round is already being dispatched. Cost if wrong: one edited sentence.
Task 28: Minor 1 DEFERRED to the final review — CLAUDE.md says internal/auth "may import db" while go list -deps shows production auth does not (only its _test.go files do; store.go takes a bare *sql.DB). Deferred because the wording is copied VERBATIM from the plan's Global Constraints and states a PERMISSION, not a crossed boundary, and it mirrors a pre-existing doc comment at internal/auth/errors.go:5.
Task 28: Minor 2's sibling also noted — the implementer self-reported leaving Project Overview alone; that self-report is what the reviewer independently confirmed, so the implementer's own honesty surfaced it.
Task 28: fix round 1/5 dispatched — fresh task-implementer (session-6 implementer unreachable), model sonnet, three scoped items (A gate re-run with captured output, B report record correction, C CLAUDE.md Project Overview sentence). Explicit timeouts ordered per the brief's slow-suite note.
Task 28: fix round 1 implementer STOPPED MID-WORK without reporting — it completed Item C (CLAUDE.md modified, uncommitted), ran make lint, launched make test in the background, then ended its turn to "wait for the background task notification", which a subagent cannot receive. Nothing was committed. MY INSTRUCTION CAUSED THIS: I told it to prefer run_in_background for the slow gate commands, which is correct advice for ME and wrong advice for a SUBAGENT — a subagent that backgrounds its critical path has no way to be woken. Correction for any future dispatch: subagents run long commands in the FOREGROUND with an explicit long timeout, never backgrounded.
Task 28: SendMessage is confirmed unavailable in this harness (ToolSearch finds no such tool), so the stopped agent cannot be resumed; this matches the session-6 note.
Task 28: DIVISION OF LABOUR CHANGED to route around it. I am running the five-command gate MYSELF as a background job writing .superpowers/sdd/plan/task-28-gate.log with an echoed exit code after each command. This is not me fixing a finding — it is verification, the one thing a controller may legitimately run, and the guard explicitly endorses run_in_background for exactly this. A fresh implementer then owns only the record work: the report evidence section, Item B, finishing Item C, and the commit.
Task 28: CORRECTION TO THE PRECEDING ENTRY — the fix-round implementer is NOT orphaned and was never stalled. It gets woken by its own background command completions, and each notification showed real progress (make lint, then make test, then make build). My diagnosis was wrong.
Task 28: THE LOG INTERLEAVING WAS MY FAULT, not a second stray loop. I truncated .superpowers/sdd/plan/task-28-gate.log and started a SECOND five-command gate loop against the same file and the same worktree while the implementer's loop was still running — two writers, and two concurrent builds racing on the same apps/backend/internal/ui/dist output. I have stopped my loop (TaskStop) and will NOT kill the implementer's processes; killing them would destroy the round's work to fix a problem I introduced.
Task 28: CONSEQUENCE TO WATCH — my truncation destroyed the implementer's earlier make lint evidence, and any output written while both loops overlapped is untrustworthy. When the implementer reports, the gate log must be treated as CONTAMINATED: either it re-runs the gate cleanly as a single writer, or the evidence section is rebuilt from one uncontaminated run. I will not accept an exit code that was recorded during the overlap window.
Task 28: PROCESS LESSON for this session — the controller running a verification command that the live implementer is ALSO running is not a safe division of labour, however legitimate verification is in the abstract. Concurrency on a shared worktree is the hazard, not authority over the command.
Task 28: fix round 1 implementer DONE, commit f916444 "docs(task-005): fix round 1 for task 28 — gate evidence, record correction, CLAUDE.md fix". Branch verified task-005-hosted-multi-user-mode. All five gate commands exit 0 — lint, test, test-integration, build AND docker-build — on a SECOND clean serialized pass. Only CLAUDE.md committed; the gate log is gitignored and referenced rather than committed.
Task 28: THE IMPLEMENTER DISCARDED THE CONTAMINATED RUN ON ITS OWN INITIATIVE and re-ran the gate serially rather than reporting exit codes from the overlap window I created. That is exactly the standard I was going to impose, reached without being told, and it is the behaviour that makes this round's evidence worth anything. Its stated concern (garbled first run, "not root-caused further") is accurate as far as it goes; the root cause is known to ME and is recorded above — it was my parallel loop, not a sandbox quirk.
Task 28: Item B resolved WITHOUT a doc change, as ruled — phrase located at execution-notes-4.md:137, and the implementer independently confirmed against authmw.go/router.go that correction #3 (api-contracts.md:115) already resolves it. Item C verified against config.go:38,251 rather than from memory, and Architecture Notes left untouched per the deferral.
Task 28: scoped re-review dispatched (model sonnet, fix range 2260abf..f916444, 1 commit / ~2 KB). The committed diff is tiny, so the review's real target is the EVIDENCE, not the code: the reviewer must read the gitignored gate log directly, confirm an explicit exit code and real output for all five commands, confirm docker-build GENUINELY ran and built an image rather than being skipped, and check the report's excerpts match the log instead of paraphrasing again.
Task 28: the re-review's single most important judgement is CONTAMINATION — I told it about my overlapping loop explicitly and ordered it to establish that the recorded exit codes come from ONE uncontaminated serialized run (no duplicated headers, no out-of-order timestamps, no command header without a matching exit code), and that failing to establish this is a NOT ADDRESSED. A clean exit code from a contaminated window is worth nothing, and I am the reason the window exists, so I am not the right judge of whether it is clean.
Task 28: re-review — ALL THREE FINDINGS ADDRESSED, no new breakage. Finding A: the log carries real output and an explicit EXIT_CODE(...)=0 for all five commands (lint 1-15, test 16-58, test-integration 59-83, build 84-445, docker-build 470-594), and the report's excerpts MATCH the log verbatim rather than paraphrasing again.
Task 28: THE CONTAMINATION QUESTION CAME BACK CLEAN AND WAS CHECKED THE WAY I ASKED — each command header appears EXACTLY ONCE, timestamps strictly increase 15:25:22 -> 15:25:31 -> 15:26:35 -> 15:27:21 -> 15:28:34 with no gaps, no duplicate headers, no header lacking an exit code, and no "Terminated" lines anywhere. The overlap window I created does not survive into the committed evidence. This is the judgement I could not make myself and it is why I ordered it.
Task 28: docker-build GENUINELY RAN — docker buildx build invocation, a real RUN CGO_ENABLED=0 go build step (#23 DONE 10.4s), image export and "importing to docker", and the strings "skip"/"not available" appear nowhere in the log. The command I flagged as the likeliest silent skip is the one with the most concrete evidence.
Task 28: Finding B independently re-verified rather than accepted — execution-notes-4.md:137 does hold the literal phrase, authmw.go:24 lists /api/auth/logout in publicRoute, :69 skips authenticate for public routes, router.go:137 registers it on the ordinary mux, and api-contracts.md:115 already says so. The reasoning holds and no doc needed changing.
Task 28: constraint checks all clean — Architecture Notes bullet at CLAUDE.md:60 present and untouched (the deferred auth/db wording preserved as ruled), no absolute home paths, trailer exact, git show --stat shows ONLY "CLAUDE.md | 2 +-" with nothing from dist/ swept in, and the gate log correctly uncommitted under .superpowers/sdd/.gitignore.
Task 28: fix round 1/5 (3 addressed, 0 open; commits 2260abf..f916444)
Task 28: complete (commits 23817e0..f916444, review clean after 1 fix round, 0 parked)
=== ALL 28 IMPLEMENTATION TASKS CLOSED (plus the mid-plan Task 30). Only Task 29, the final whole-branch review, remains. ===
Task 29: FINAL REVIEW DISPATCHED — four reviewers in parallel, base 7ae00b9..f916444 (50 commits, 140 files, +24703/-271). plan-adherence-reviewer (OPUS), backend-guidelines-reviewer (OPUS), frontend-guidelines-reviewer (sonnet), and the SDD whole-branch code reviewer (OPUS). Three opus seats is deliberate: this is the single merge gate on a 50-commit branch that adds authentication, encryption at rest and multi-tenant isolation to a previously single-user product, and the SDD rule puts the final review on the most capable model.
Task 29: RULING — each reviewer writes its OWN audit file (audit-plan-adherence.md, audit-backend.md, audit-frontend.md, audit-final.md) instead of the audit.md the brief names, and I merge them afterwards. Reasoning: the brief says "each writes findings to audit.md" and four concurrent writers to one file would clobber each other, losing findings silently — the one failure mode a merge gate cannot afford. Cost if wrong: one concatenation step and a brief that describes the output path loosely.
Task 29: RULING — EXACTLY ONE reviewer runs the gates. plan-adherence runs make lint/test/test-integration/build in the FOREGROUND with explicit 600000ms timeouts; the other three are forbidden from running any make/go test/go build/go vet/golangci-lint/npm command and were told to read task-28-gate.log instead, with the reason stated (two concurrent builds race on shared output dirs, and npm run build dirties dist/.gitkeep for everyone). This is the direct lesson from the contamination I caused earlier — the hazard is concurrency on a shared worktree, and four reviewers each independently deciding to verify is exactly how it recurs. Cost if wrong: three reviewers trust a captured log rather than their own run.
Task 29: every dispatch also carries the FOREGROUND-NOT-BACKGROUND rule for long commands, since backgrounding its critical path is what stalled the Task 28 fix implementer.
Task 29: residuals.md written as the triage artifact and handed to all four — 1 parked finding, 7 deferred minors, the Task 30 narrative correction (with an instruction to treat any surviving "logged in clear" claim on the branch as a finding), and the internal/auth flake with an explicit demand for a call rather than a lapse. The frontend reviewer was told item 4 (native <select> vs the shipped Radix Select) is the one residual with real design weight and to give a recommendation, not a both-sides summary.
Task 29: FRONTEND audit in (audit-frontend.md) — 0 Critical, 2 Important, 2 FE-* FAILs, verdict "mergeable with fast-follow". Importants: the native <select> at UserProviderForm.tsx:802-809 and :931-938, and renderPageWithClient duplicating test/render.tsx at AccountSettingsPage.test.tsx:3277-3295. FAILs: FE-03 AuthProvider.tsx:5 importing setUnauthorizedHandler straight from @/lib/api/client (401-handler registration, not a data-fetch bypass), and FE-10 types/models/auth.ts:4-13,18-26 declaring AuthMode/CurrentUser/UserProvider as flat interfaces instead of the Resource<"type", Attributes> shape every other model in types/models/ uses.
Task 29: the <select> ruling I asked for came back as a RECOMMENDATION, not a both-sides summary, and it is persuasive — FIX BEFORE MERGE. The divergence is concrete (no Radix focus ring, animation, or dark-mode theming on the native popup), the field appears in only two places with two static options, and a working reference pattern already exists in-tree at ProviderPicker.tsx. Cheap, contained, and it is the one residual with real design weight, so shipping it as-is would be choosing inconsistency for no gain.
Task 29: renderPageWithClient ruled CONSOLIDATE BUT NON-BLOCKING — the suggested shape is for test/render.tsx's renderWithProviders to expose its QueryClient the way queryWrapper already does for hooks. Fast-follow, not a merge blocker.
Task 29: FE-10 is worth more attention than its "minor" label suggests — a new model file diverging from the Resource<> shape that EVERY other model uses is the kind of inconsistency that propagates, since the next author copies the nearest example. Holding it for the fix wave decision rather than waving it through.
Task 29: per SDD the final review gets exactly ONE fix dispatch covering ALL findings from ALL four reviewers, so I am holding this wave until plan-adherence, backend and whole-branch report. Three still running.
Task 29: BACKEND audit in (audit-backend.md) — 0 Critical, 4 Important, 3 checklist FAILs + 1 WARN. EVERY BINDING SECURITY CONSTRAINT VERIFIED WITH file:line EVIDENCE: SQL parameterisation, shell-free git with env-only credentials, exact Argon2id (t=1 m=65536 p=4, 16-byte salt, 32-byte key, PHC, params read BACK from the stored string), AES-256-GCM with per-record nonce and user_id+row-id AAD, converge_session as base64url-32-bytes with only its SHA-256 persisted, redaction, dependency direction, modernc sqlite with no mattn, all ten code/status pairs verbatim, and multi-tenant isolation. NO PATH FOUND by which one user reaches another's data, a secret reaches a log, or a 403-vs-404 discloses existence. That is the verdict the branch was built for.
Task 29: PLAN-ADHERENCE audit in (audit-plan-adherence.md) — 29/29 tasks adherent (1-28 plus 30), 0 skipped, 0 stubbed, 0 partial. All 177 Test* identifiers named anywhere in the 7124-line plan EXIST in the backend; all 59 numbered frontend assertions exist. Tasks 17/18's deliberately-skipped tests are un-skipped and assert substantively. Task 5's argon2.IDKey test survived un-"simplified".
Task 29: THE STANDALONE-EQUIVALENCE DISCIPLINE HELD, and this is the check I cared most about on the whole branch. The reviewer read EVERY REMOVED LINE in every pre-existing backend test file: all are mechanical call-site signature substitutions, and no assertion was added, removed, or retargeted in any pre-existing test body. config_test.go is +143/-0. A pass of the old suite therefore means what it appears to mean, which is the entire premise of requirement #1.
Task 29: gates PASS on an independent foreground run — make lint (0 issues), make test (22 Go pkgs ok, frontend 42 files/337 tests), make test-integration (22 pkgs ok), make build (both binaries, both arches). docker-build read from task-28-gate.log rather than re-run, as instructed.
Task 29: TWO REVIEWERS INDEPENDENTLY FOUND THE SAME DEFECT, which is the strongest signal of the wave — docs/hosted-mode.md:106-110 CONTRADICTS SHIPPED CODE. It warns operators that converge-cli would open the hosted DB if CONVERGE_MODE=hosted were inherited, but cmd/converge-cli/main.go:80 appends CONVERGE_MODE=standalone so the later duplicate wins, and main_test.go:154-176 asserts no DB file is created. The doc actively misinstructs operators about a safety property the code already guarantees.
Task 29: RESIDUAL 8 IS CORRECTED BY THIS, and the backend reviewer was right to say it differs from my recorded ruling. I carried residual 8 forward as "a code residual documented rather than fixed". The truth is the inverse: the CODE residual was CLOSED and only the DOC was left behind asserting it still exists. My framing of that residual was wrong.
Task 29: RULING — DOM-01 and DOM-IMM (no builder.go in internal/auth; UpdateProvider assigning six fields directly) DEFERRED as conformance debt, not taken into the fix wave. GROUNDED, not assumed: I read providers.go:99-155 myself. `row` is a LOCAL COPY of an immutable value type returned by Store.UserProviderByID, so nothing shared is mutated and immutability holds in substance — what is missing is the prescribed builder IDIOM. The plan's File Structure enumerated internal/auth's files exhaustively and did not include builder.go, so this is the plan's shape, and CLAUDE.md already records that the backend deviates from the guidelines where they assume other infrastructure. Cost if wrong: internal/auth keeps a construction idiom inconsistent with internal/session until a follow-up.
Task 29: RULING — the providerResolver residency finding GOES INTO THE FIX WAVE, and as BOTH halves, not just the cheap one. Grounded by reading resolver.go:15-23 and :61-94 myself: the TTL is consulted only on the READ path (`now.Sub(entry.builtAt) < providerResolverTTL`), and the only delete is Invalidate, so an elapsed entry is never evicted — a user who logs in once leaves decrypted tokens resident in an unbounded map for the process lifetime. The comment at :15-16 claims the TTL "bounds how long a user's decrypted tokens sit in memory", which is FALSE AS WRITTEN; it bounds how long they are USED. So the branch states an invariant it does not deliver, on a secret-residency property, in a hosted multi-user product. Requiring only the comment fix would be choosing to document the weaker behaviour rather than provide the stated one. Cost if wrong: an eviction pass and its test, in a cache that is already mutex-guarded.
Task 29: flake — BOTH reviewers independently called it RETIRED, and the backend reviewer did the thing I valued most by looking for a MECHANISM instead of running it again: no shared clock, no shared temp dir, no unsynchronised counter, and the only package-level mutable state is dummyHash (password.go:183) written once at init. Plan-adherence adds that 32f668e's atomic-counter fix is a plausible cause for a load-dependent flake. The one real coupling named is ~320 MiB transient Argon2 (64 MiB x hashConcurrency 4) under parallel package load, which would surface as a TIMEOUT OR OOM and never as a wrong assertion — so if it ever returns, the first question is which of the two it is. RULING: flake retired, with that discriminator recorded for the next person.
Task 29: WHOLE-BRANCH audit in (audit-final.md) — verdict "merge after four Importants", 0 Critical, 10 Minor. Standalone equivalence confirmed GENUINELY EVIDENCED, not asserted: app_test.go:378-406 asserts DB == nil AND Auth == nil AND os.Stat on BOTH the configured and default db paths, and isolation_test.go:377-407 walks all six hosted routes for 404. Architecture re-confirmed by go list -deps.
Task 29: THE OPUS WHOLE-BRANCH REVIEWER EARNED ITS SEAT — it found two behavioural defects in internal/auth that the dedicated backend checklist reviewer, also on opus, did not. This is the argument for keeping a broad merge review distinct from a checklist audit: the checklist asked "is each constraint satisfied" and the merge review asked "does this system behave".
Task 29: I4 — THE MOST SERIOUS FINDING OF THE ENTIRE BRANCH, AND I AM ESCALATING IT ABOVE THE REVIEWER'S OWN LABEL. store.go:429-432's sweeper has NO scope predicate, so it reaps user-scope failure counters once 15 minutes idle, directly contradicting throttle.go:83-84 and FR-7.2 ("resets only on success, no window"). Consequence: AN ATTACKER PACING GUESSES MORE THAN 15 MINUTES APART NEVER REACHES THE 5-FAILURE LOCKOUT. The lockout is this branch's primary defence against credential guessing and as shipped it is bypassable by waiting. RULING: treat as Critical-equivalent, first item of the wave, failing test required before the fix. Cost if wrong: one narrowed SQL predicate and a test.
Task 29: I3 — login_attempts rows are not cleaned on DeleteUser and store.go:107-109's comment CLAIMS A CASCADE THAT 0001_init.sql:47-54 DOES NOT DECLARE. A username lockout therefore survives account deletion and is inherited by whoever re-registers that username. Real behaviour, plus a false comment asserting the opposite.
Task 29: I2 — UserProviderForm.tsx:73-158 vs :196-281, ~75 of 86 lines of form body duplicated VERBATIM between create and edit. Verbatim duplication of a logic block is named explicitly in the review rubric as Important, so it goes in the wave rather than being waved through as style.
Task 29: THE FLAKE CALL IS REVERSED, AND I AM OVERRIDING TWO REVIEWERS WITH THE THIRD. Plan-adherence and backend both called it retired on ~22 clean runs. The whole-branch reviewer NAMED A MECHANISM instead: service_test.go:224-257 is a wall-clock MEDIAN-OF-THREE 2x RATIO assertion running under t.Parallel() with -race, where each sample can queue behind other parallel tests' 64 MiB Argon2 hashes through the 4-slot hashConcurrency semaphore (service.go:22). That explains the exact observed signature — fails under full-suite load, never in a focused -count=3 run, BECAUSE THE FOCUSED RUN REMOVES THE CONTENTION THAT CAUSES IT. RULING: the flake is NOT retired; it is a test-robustness defect with a named cause, and counting clean runs cannot outweigh a mechanism that predicts the observed pattern. Into the wave, preserving what the test guards (a timing-oracle guard on whether the user exists), not deleting it. Cost if wrong: a timing assertion rewritten to assert the dummy-hash code path instead of wall-clock.
Task 29: M1 — the retired "logged in clear" framing SURVIVES at github/authorize_git_secret_test.go:23-24 even though the body below it was corrected. My instruction to treat a surviving instance as a finding is what caught it.
Task 29: RULING — deferred from the wave with reasons recorded: DOM-01/DOM-IMM (grounded above), FE-03, FE-10 (a real consistency wart, but reshaping types/models/auth.ts touches the API typing surface and a final fix wave is the wrong place to do that), renderPageWithClient consolidation, the plan's 174 unticked checkboxes and f916444's message describing a gitignored log as committed (record-keeping, and the ledger plus four audits are the record). Cost if wrong: FE-10's flat interfaces get copied by the next author before the follow-up lands.
Task 29: fix wave dispatched as ONE implementer (OPUS, task-implementer) against a written brief at .superpowers/sdd/plan/final-fix-brief.md rather than a pasted findings list — ten ordered items, scope exclusions stated explicitly, failing-test-first required for items 1/2/4, and an instruction that a technically wrong finding gets a REASONED REBUTTAL in audit.md rather than a change. It also owns the post-fix gate re-run (foreground, captured, per-command exit codes), assembling audit.md from the four reports for Task 29 Step 4, and the commit.
Task 29: FIX WAVE DONE_WITH_CONCERNS, commit 2c06b47 "fix(task-005): close the final whole-branch review findings". Branch verified, tree clean. All five gates exit 0 in the foreground at this exact commit with captured output at final-fix-gate.log. 108 tool calls of its 120 budget — it finished inside the cap rather than handing back PARTIAL.
Task 29: nine of ten items implemented, ONE REBUTTED. Item 10a (422 echoing raw err.Error()) was rebutted with evidence rather than changed — the implementer's claim is that all six ErrInvalidInput messages on that path are static and auth-authored and that provider failures take a different arm. This is exactly the behaviour I asked for: a reasoned "no" with evidence beats a change that papers over a misunderstanding. It is NOT accepted yet; the re-review must adjudicate the rebuttal on its merits.
Task 29: the failing-test-first requirement was MET AND QUOTED — 5/5 defects for items 1/2/4/10b reproduced before fixing, and the mutation ratios cut both ways (M1 skip-dummy-verify 1/1 with the quoted failure "allocated 9664 bytes, below the 50331648-byte floor"; M2+M3 evict-everything and drop-scope-predicate 4/4). All mutations done in /tmp scratch copies, never in the tree.
Task 29: item 8's fix is the one I find most interesting and most in need of checking — the wall-clock ratio was replaced by an ALLOCATION MEASUREMENT with a ~48 MiB floor, the test de-parallelised, a PHC-parameter equality test added, and the 200 ms deadline raised to 10 s. An allocation floor is a deterministic proxy for "the expensive hash path ran", which is strictly better than wall-clock under parallel load. But a proxy can be sound or can quietly weaken the guard, so I routed it to the re-review with the precise question: would this still fail if the dummy-hash path were skipped? The implementer's own M1 mutation says yes; the reviewer checks that independently.
Task 29: CONCERN 1 IS THE ONE THAT MATTERS AND IS NOW THE RE-REVIEW'S HEADLINE — four PRE-EXISTING internal/auth tests asserted the DEFECTIVE semantics and were rewritten, so a green run in that package no longer means what it meant at f916444. The plan forbids changing assertions in pre-existing tests while changing the code under them, and the branch's whole requirement-#1 argument rests on that discipline. The tension is real and unavoidable here: the old tests encoded the very defect item 1 fixes, so SOMETHING had to change. I ordered a verdict PER REWRITTEN TEST — did the old assertion genuinely encode the defect, or was the test bent to fit the new code — plus independent verification of the claim that no standalone test was touched.
Task 29: concern 4 routed as a potential CRITICAL — deps.Resolver is dereferenced in Sweep with no nil guard, deliberately. A nil-pointer panic in a periodic background sweeper is a production crash, so the reviewer must prove no wiring path (standalone, CLI, tests) reaches Sweep with a nil Resolver rather than accept that it happens not to today.
Task 29: concern 3 noted as a TRADE I BELIEVE FR-7.2 REQUIRES — user-scope counters now persist until successful login or account deletion, which is a bounded growth vector, but "resets only on success, no window" is precisely what the spec says. Routed for confirmation that the growth is genuinely bounded.
Task 29: concern 5 — two mechanical edits made BY SCRIPT rather than the Edit tool, contrary to this project's standing instruction. Self-reported, which is the right behaviour. Process deviation; the re-review verifies the results landed correctly and it is a Minor if they did.
Task 29: single scoped re-review dispatched (OPUS, fix range f916444..2c06b47, 308 KB) and told explicitly that there is NO SECOND FIX WAVE after it, so a real problem it passes over ships. Forbidden from re-running the gate (all five already captured at this commit) and from re-reviewing the rest of the branch.
Task 29: RE-REVIEW — ALL FINDINGS ADDRESSED, SHIPS: YES. Nine items implemented and verified, item 10a's rebuttal adjudicated SOUND (five ErrInvalidInput producers, all auth-authored literals with only %w interpolated, no other package references the sentinel, provider failures take writeDomainError's fixed arm — the brief itself miscounted them as six). No new breakage in the fix diff.
Task 29: ITEM 1's NARROWING CONFIRMED CORRECT RATHER THAN MERELY DIFFERENT, which was the precise question I asked. ipWindow is the only window in the design; FR-7.3 gives the IP counter a 15-minute window while FR-7.2 gives the username counter none; so reaping a user row would ITSELF BE the forbidden reset. Regression test at throttle_sweep_test.go:22-52 (paced failures across sweeps still lock at 5) PLUS an over-fix guard at :59-82 proving a stale IP row is still reaped. Expired login sessions untouched, FR-3.7 intact.
Task 29: CONCERN 1 ADJUDICATED PER-TEST AS ORDERED, AND ALL FOUR REWRITES ARE NECESSARY AND CORRECT — none bent to fit. The most striking: store_test.go:340-355's old TestDeleteUserCascades asserted `Failures != 1` must FAIL, i.e. THE ORPHANED ROW MUST SURVIVE, with a comment rationalising it as "a documentation assertion, not a defect". That is the defect written down as an expectation. TestDeleteElapsedAttempts' old fixtures were both ScopeUser and REQUIRED the elapsed user row to be deleted — the bypass itself; the rewrite moves the pair to ScopeIP and ADDS an assertion that a user row survives, strictly stronger. All four new assertions are stronger than their predecessors and each pins the fix's narrowness.
Task 29: STANDALONE EQUIVALENCE HOLDS BY CONSTRUCTION, independently verified — the fix diff contains no standalone-path file, and internal/auth is constructed ONLY inside the cfg.Mode == config.ModeHosted branch at app.go:309-341. Requirement #1 survives the fix wave.
Task 29: concern 4 CLEARED as not-Critical with a real proof rather than an observation — Sweep is reachable only via AuthSweep, set only at cmd/converge/main.go:62 from authSweep(), which returns nil when a.Auth == nil, and router.go:90 starts the goroutine only when AuthSweep != nil. Production has exactly one ServiceDeps construction and it sets Resolver. No CLI path constructs auth. No nil path exists.
Task 29: item 8's allocation proxy JUDGED SOUND ON ITS MERITS AND STRONGER THAN WHAT IT REPLACED — the failure mode the original guarded (unknown-username arm returning before hashing) drops allocation to kilobytes and fails loudly, background noise can only push a floor measurement UP so a floor is safe against it, and the equal-COST half the 2x wall-clock ratio only approximated is now asserted EXACTLY by password_internal_test.go:8-30 comparing decodePHC(dummyHash) params against a real hash's. It would not pass with the dummy path skipped.
Task 29: RULING — the two residual minors the re-review surfaced are PARKED AND SURFACED TO THE USER, not fixed. SDD allows exactly one fix wave and explicitly routes residual findings to the human at finishing-a-development-branch. The substantive one is api/errors.go:70's default 500 arm returning the code GIT_FAILURE, which item 10b NEWLY made reachable for a database fault — so a DB outage is now reported to the client as GIT_FAILURE (status and detail right, code string misleading). Introduced by this wave's own fix and named first in the residual list as the next thing to fix. The other is settings_providers.go:176 saying "four" request-shape failures where there are five. Cost if wrong: one misleading error code string reaches a PR review instead of being fixed here.
Task 29: complete (commits f916444..2c06b47, all findings addressed, 2 residual minors parked and surfaced)
=== TASK 29 CLOSED. THE PLAN IS COMPLETE: 28 implementation tasks, Task 30, and the final review. ===
Session 7: ledger preserved into git at docs/tasks/task-005-hosted-multi-user-mode/sdd-ledger.md (460 lines, 43 rulings, verified free of absolute home paths) and session notes written to execution-notes-7.md, BEFORE deleting the workspace — otherwise every ruling in this file would die with it.
